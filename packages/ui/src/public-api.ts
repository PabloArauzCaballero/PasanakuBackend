// Sin barril a propósito (conflicto nº 2 de planes/16): cada componente se importa por su ruta,
// `@aportaya/ui/<componente>/<componente>`. Este archivo existe porque ng-packagr exige un punto
// de entrada; no reexporta nada.
export {}
