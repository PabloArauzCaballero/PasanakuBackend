00:00 +0: loading C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/cliente_refresco_test.dart
00:00 +0: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/cliente_refresco_test.dart: diez 401 concurrentes con token vencido comparten un único refresco
00:00 +1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/cliente_refresco_test.dart: si el refresco falla, la sesión se cierra una vez y las N fallan con su error original
00:00 +2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/cliente_test.dart: un 403 llega traducido, con traza y sin el mensaje del backend
00:00 +3: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/cliente_test.dart: sin red llega como ErrorDeRed
00:01 +4: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enchufe_de_rutas_test.dart: el enchufe del shell importa las rutas de cada dominio, y nada más las conoce
00:01 +5: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enchufe_de_rutas_test.dart: agregar una pantalla vacía en un dominio no cambia ningún archivo fuera de él
00:01 +5 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enlaces_profundos_test.dart: aportaya://unirse/ABC123 abre la ruta de unirse a un pasanaku
00:01 +6 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enlaces_profundos_test.dart: aportaya://billetera sin resto va al inicio de billetera
00:01 +7 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enlaces_profundos_test.dart: un esquema distinto de aportaya no se traduce
00:01 +8 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enlaces_profundos_test.dart: un host sin traducción conocida no se traduce
00:01 +9 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enchufe_de_rutas_test.dart: agregar una pantalla vacía en un dominio no cambia ningún archivo fuera de él [E]
  Expected: {
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\app.dart': 'import \'dart:async\';\r\n'
                '\r\n'
                'import \'package:flutter/material.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                'import \'package:go_router/go_router.dart\';\r\n'
                '\r\n'
                'import \'dominio/verificacion_contrato.dart\';\r\n'
                'import \'navegacion/rutas.dart\';\r\n'
                'import \'pantallas/soporte/capa_de_tutorial.dart\';\r\n'
                'import \'proveedores/sesion.dart\';\r\n'
                'import \'package:aportaya_diseno/moviles/apertura_de_marca.dart\';\r\n'
                'import \'package:aportaya_diseno/moviles/anclas_de_tutorial.dart\';\r\n'
                'import \'package:aportaya_diseno/moviles/como_en_un_telefono.dart\';\r\n'
                'import \'package:aportaya_diseno/tema.dart\';\r\n'
                'import \'package:aportaya_diseno/tokens/tokens.dart\';\r\n'
                '\r\n'
                'class AppAportaYa extends ConsumerWidget {\r\n'
                '  /// [inicial] nulo significa «la que diga el enrutador». Antes esta clase repetía su\r\n'
                '  /// propia ruta de arranque, y cuando la del enrutador cambió a la portada, esta\r\n'
                '  /// copia siguió mandando a la billetera: la app abría adentro igual. Un valor por\r\n'
                '  /// defecto en dos lugares es un valor por defecto que va a divergir.\r\n'
                '  AppAportaYa({super.key, String? inicial})\r\n'
                '    : _enrutador = inicial == null\r\n'
                '          ? crearEnrutador()\r\n'
                '          : crearEnrutador(inicial: inicial);\r\n'
                '\r\n'
                '  final GoRouter _enrutador;\r\n'
                '\r\n'
                '  @override\r\n'
                '  Widget build(BuildContext context, WidgetRef ref) {\r\n'
                '    // Se dispara al construir el árbol, sin bloquear el primer frame: si el\r\n'
                '    // gateway está caído al iniciar, el arranque no se cuelga esperándolo.\r\n'
                '    ref.watch(verificacionContratoProvider);\r\n'
                '    return MaterialApp.router(\r\n'
                '      title: \'AportaYa\',\r\n'
                '      theme: temaDesde(Tokens.claro, Brightness.light),\r\n'
                '      darkTheme: temaDesde(Tokens.oscuro, Brightness.dark),\r\n'
                '      themeMode: ThemeMode.system,\r\n'
                '      routerConfig: _enrutador,\r\n'
                '      debugShowCheckedModeBanner: false,\r\n'
                '      // El registro de anclas y la capa del tutorial envuelven a TODA la app: un\r\n'
                '      // recorrido cruza pantallas, así que no puede vivir dentro de una. Con el\r\n'
                '      // tutorial apagado, las dos piezas no dibujan ni escuchan nada.\r\n'
                '      builder: (context, child) => ComoEnUnTelefono(\r\n'
                '        child: ProveedorDeAnclas(\r\n'
                '          hijo: CapaDeTutorial(\r\n'
                '            enrutador: _enrutador,\r\n'
                '            hijo: _ConApertura(\r\n'
                '              enrutador: _enrutador,\r\n'
                '              child: _AvisoDeContrato(child: child),\r\n'
                '            ),\r\n'
                '          ),\r\n'
                '        ),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// Una franja angosta arriba, no un diálogo que tapa la pantalla: el aviso de\r\n'
                '/// «actualizá la app» no puede impedir seguir usando lo que ya funciona.\r\n'
                'class _AvisoDeContrato extends ConsumerWidget {\r\n'
                '  const _AvisoDeContrato({required this.child});\r\n'
                '  final Widget? child;\r\n'
                '\r\n'
                '  @override\r\n'
                '  Widget build(BuildContext context, WidgetRef ref) {\r\n'
                '    final estado = ref.watch(verificacionContratoProvider).asData?.value;\r\n'
                '    if (estado != EstadoContrato.desactualizado || child == null) {\r\n'
                '      return child ?? const SizedBox.shrink();\r\n'
                '    }\r\n'
                '    return Column(\r\n'
                '      children: [\r\n'
                '        Material(\r\n'
                '          color: Theme.of(context).colorScheme.errorContainer,\r\n'
                '          child: SafeArea(\r\n'
                '            bottom: false,\r\n'
                '            child: Padding(\r\n'
                '              padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),\r\n'
                '              child: Text(\r\n'
                '                \'Hay una versión nueva de AportaYa. Actualizala para seguir \'\r\n'
                '                \'operando sin problemas.\',\r\n'
                '                style: Theme.of(context).textTheme.bodySmall,\r\n'
                '              ),\r\n'
                '            ),\r\n'
                '          ),\r\n'
                '        ),\r\n'
                '        Expanded(child: child!),\r\n'
                '      ],\r\n'
                '    );\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// La apertura de marca por encima de la app, una sola vez por arranque.\r\n'
                '///\r\n'
                '/// Va acá arriba y no como una ruta: así la app **ya está armada y pidiendo el saldo**\r\n'
                '/// detrás de la animación, en vez de empezar a cargar recién cuando la apertura\r\n'
                '/// termina. La animación tapa el arranque; no lo demora.\r\n'
                '/// También es el momento en que se decide **a dónde entrar**: mientras la marca se\r\n'
                '/// acerca, se lee el almacén seguro. Si hay una sesión guardada, la app abre en la\r\n'
                '/// billetera; si no, se queda en la portada. Es el único lugar donde esa pregunta se\r\n'
                '/// hace, y se hace una sola vez por arranque: preguntarla en cada navegación sería\r\n'
                '/// tocar el llavero del teléfono a cada paso.\r\n'
                'class _ConApertura extends ConsumerStatefulWidget {\r\n'
                '  const _ConApertura({required this.child, required this.enrutador});\r\n'
                '  final Widget child;\r\n'
                '  final GoRouter enrutador;\r\n'
                '\r\n'
                '  @override\r\n'
                '  ConsumerState<_ConApertura> createState() => _ConAperturaState();\r\n'
                '}\r\n'
                '\r\n'
                'class _ConAperturaState extends ConsumerState<_ConApertura> {\r\n'
                '  bool _mostrando = true;\r\n'
                '  Timer? _respaldo;\r\n'
                '\r\n'
                '  @override\r\n'
                '  void initState() {\r\n'
                '    super.initState();\r\n'
                '    _decidirDondeEntrar();\r\n'
                '    // El cinturón de seguridad: si la animación no llamara a su final —un ticker que\r\n'
                '    // no arranca, un frame que no llega—, a los dos segundos la apertura se va igual.\r\n'
                '    // Nadie se queda mirando un logo sin poder entrar a su plata.\r\n'
                '    _respaldo = Timer(const Duration(seconds: 2), _ocultar);\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> _decidirDondeEntrar() async {\r\n'
                '    // Si leer el llavero falla —teléfono con el almacén bloqueado, un permiso raro—,\r\n'
                '    // lo seguro es la portada: se pide ingresar de nuevo. Nunca al revés.\r\n'
                '    String? token;\r\n'
                '    try {\r\n'
                '      token = await ref.read(sesionProvider).tokenDeAcceso();\r\n'
                '    } on Object {\r\n'
                '      token = null;\r\n'
                '    }\r\n'
                '    if (!mounted || token == null || token.isEmpty) return;\r\n'
                '    if (widget.enrutador.state.uri.toString() == \'/portada\') {\r\n'
                '      widget.enrutador.go(\'/billetera/inicio\');\r\n'
                '    }\r\n'
                '  }\r\n'
                '\r\n'
                '  void _ocultar() {\r\n'
                '    _respaldo?.cancel();\r\n'
                '    if (!mounted || !_mostrando) return;\r\n'
                '    // Con «reducir movimiento», la apertura pide cerrarse mientras se está\r\n'
                '    // construyendo el árbol, y ahí `setState` sobre un ancestro es un error de\r\n'
                '    // Flutter. Se difiere al frame siguiente, que además es cuando corresponde.\r\n'
                '    WidgetsBinding.instance.addPostFrameCallback((_) {\r\n'
                '      if (!mounted || !_mostrando) return;\r\n'
                '      setState(() => _mostrando = false);\r\n'
                '    });\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  void dispose() {\r\n'
                '    _respaldo?.cancel();\r\n'
                '    super.dispose();\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  Widget build(BuildContext context) => Stack(\r\n'
                '    children: [\r\n'
                '      widget.child,\r\n'
                '      if (_mostrando)\r\n'
                '        Positioned.fill(child: AperturaDeMarca(alTerminar: _ocultar)),\r\n'
                '    ],\r\n'
                '  );\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\catalogo.dart': 'import \'package:flutter/material.dart\';\r\n'
                '\r\n'
                'import \'package:aportaya_diseno/catalogo/catalogo.dart\';\r\n'
                '\r\n'
                '/// Punto de entrada del catálogo del sistema de diseño en dispositivo:\r\n'
                '///   flutter run -t lib/catalogo.dart\r\n'
                'void main() => runApp(const CatalogoDeDiseno());\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cliente.dart': 'import \'dart:math\';\r\n'
                '\r\n'
                'import \'package:dio/dio.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/errores.dart\';\r\n'
                'import \'../proveedores/sesion.dart\';\r\n'
                '\r\n'
                '/// **Una sola base URL: el gateway.** El prefijo enruta al servicio; la app no\r\n'
                '/// conoce catorce direcciones.\r\n'
                '///\r\n'
                '/// Por omisión apunta al **backend de verdad** en la máquina de desarrollo: NGINX\r\n'
                '/// publica el 80, y el gateway enruta `/api/v1/<prefijo>` al servicio que reservó ese\r\n'
                '/// prefijo. Antes apuntaba a Prism, el simulado, y con eso la app «funcionaba» sin\r\n'
                '/// que existiera un backend detrás: cualquier celular y cualquier contraseña entraban.\r\n'
                '///\r\n'
                '/// El simulado sigue disponible para pruebas de contrato, pero hay que pedirlo:\r\n'
                '/// `--dart-define=API=http://localhost:4010/api/v1`. Desde el emulador de Android la\r\n'
                '/// máquina es `10.0.2.2`; desde un teléfono real, la IP de la red local.\r\n'
                'const String baseDelGateway = String.fromEnvironment(\r\n'
                '  \'API\',\r\n'
                '  defaultValue: \'http://localhost/api/v1\',\r\n'
                ');\r\n'
                '\r\n'
                '/// La única salida a la red de la app. Ningún widget crea un `Dio`.\r\n'
                'final dioProvider = Provider<Dio>((ref) {\r\n'
                '  final dio = Dio(\r\n'
                '    BaseOptions(\r\n'
                '      baseUrl: baseDelGateway,\r\n'
                '      connectTimeout: const Duration(seconds: 8),\r\n'
                '      receiveTimeout: const Duration(seconds: 12),\r\n'
                '      headers: {\'Accept\': \'application/json\'},\r\n'
                '    ),\r\n'
                '  );\r\n'
                '  instalarInterceptores(dio, ref.read(sesionProvider));\r\n'
                '  return dio;\r\n'
                '});\r\n'
                '\r\n'
                '/// Los mismos interceptores en la app y en las pruebas: lo que se prueba es la\r\n'
                '/// traducción de errores y la traza reales, no un doble.\r\n'
                'void instalarInterceptores(Dio dio, Sesion sesion) {\r\n'
                '  // Mismo transporte (adaptador) que `dio` — así las pruebas que instalan un\r\n'
                '  // adaptador simulado en `dio` también cubren el refresco — pero SIN sus\r\n'
                '  // interceptores: `_TrazaYSesion` no debe reentrar sobre su propio refresco.\r\n'
                '  final dioDeRefresco = Dio(BaseOptions(baseUrl: dio.options.baseUrl))\r\n'
                '    ..httpClientAdapter = dio.httpClientAdapter;\r\n'
                '  final refrescador = Refrescador(sesion, dioDeRefresco);\r\n'
                '  dio.interceptors.add(_TrazaYSesion(sesion, dio, refrescador));\r\n'
                '  dio.interceptors.add(_TraduccionDeErrores());\r\n'
                '}\r\n'
                '\r\n'
                '/// Un solo refresco en vuelo por vez: N peticiones que reciben `401` a la vez\r\n'
                '/// comparten el mismo `Future` en vez de disparar un `POST /sesion/refrescar` cada\r\n'
                '/// una. Sale por un `Dio` propio, sin los interceptores de la app: si pasara por\r\n'
                '/// `_TrazaYSesion`, un `401` del propio refresco reentraría a este mismo mecanismo.\r\n'
                'class Refrescador {\r\n'
                '  Refrescador(this._sesion, this._dioSinInterceptores);\r\n'
                '  final Sesion _sesion;\r\n'
                '  final Dio _dioSinInterceptores;\r\n'
                '  Future<bool>? _enVuelo;\r\n'
                '\r\n'
                '  Future<bool> refrescar() => _enVuelo ??= _hacerRefresco().whenComplete(() {\r\n'
                '    _enVuelo = null;\r\n'
                '  });\r\n'
                '\r\n'
                '  Future<bool> _hacerRefresco() async {\r\n'
                '    final refresco = await _sesion.tokenDeRefresco();\r\n'
                '    if (refresco == null) return false;\r\n'
                '    try {\r\n'
                '      final r = await _dioSinInterceptores.post<Map<String, dynamic>>(\r\n'
                '        \'/sesion/refrescar\',\r\n'
                '        data: {\'refresco\': refresco},\r\n'
                '      );\r\n'
                '      final acceso = r.data?[\'acceso\'] as String?;\r\n'
                '      final nuevo = r.data?[\'refresco\'] as String?;\r\n'
                '      if (acceso == null || nuevo == null) return false;\r\n'
                '      await _sesion.guardar(acceso: acceso, refresco: nuevo);\r\n'
                '      return true;\r\n'
                '    } on DioException {\r\n'
                '      return false;\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// `x-request-id` en cada petición, bearer desde el almacén seguro, y **un** refresco\r\n'
                '/// compartido (single-flight, `Refrescador`) con **un** reintento por petición. Si\r\n'
                '/// falla, sesión cerrada, no bucle.\r\n'
                'class _TrazaYSesion extends Interceptor {\r\n'
                '  _TrazaYSesion(this._sesion, this._dio, this._refrescador);\r\n'
                '  final Sesion _sesion;\r\n'
                '  final Dio _dio;\r\n'
                '  final Refrescador _refrescador;\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<void> onRequest(\r\n'
                '    RequestOptions options,\r\n'
                '    RequestInterceptorHandler handler,\r\n'
                '  ) async {\r\n'
                '    options.headers[\'x-request-id\'] ??= _traza();\r\n'
                '    final token = await _sesion.tokenDeAcceso();\r\n'
                '    if (token != null) options.headers[\'Authorization\'] = \'Bearer $token\';\r\n'
                '    handler.next(options);\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<void> onError(\r\n'
                '    DioException err,\r\n'
                '    ErrorInterceptorHandler handler,\r\n'
                '  ) async {\r\n'
                '    final yaReintentada = err.requestOptions.extra[\'reintentada\'] == true;\r\n'
                '    if (err.response?.statusCode != 401 || yaReintentada) {\r\n'
                '      return handler.next(err);\r\n'
                '    }\r\n'
                '    final refrescada = await _refrescador.refrescar();\r\n'
                '    if (!refrescada) {\r\n'
                '      await _sesion.cerrar();\r\n'
                '      return handler.next(err);\r\n'
                '    }\r\n'
                '    final opciones = err.requestOptions..extra[\'reintentada\'] = true;\r\n'
                '    try {\r\n'
                '      handler.resolve(await _dio.fetch(opciones));\r\n'
                '    } on DioException catch (e) {\r\n'
                '      if (e.response?.statusCode == 401) {\r\n'
                '        await _sesion.cerrar();\r\n'
                '      }\r\n'
                '      handler.next(e);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// Un error de red no llegó al backend: no lleva código de seguimiento. Un error de\r\n'
                '/// API llega con `AP-CU<NN>-<nn>` y se traduce por catálogo, nunca se muestra crudo.\r\n'
                'class _TraduccionDeErrores extends Interceptor {\r\n'
                '  @override\r\n'
                '  void onError(DioException err, ErrorInterceptorHandler handler) {\r\n'
                '    final respuesta = err.response;\r\n'
                '    if (respuesta == null) {\r\n'
                '      return handler.reject(\r\n'
                '        DioException(\r\n'
                '          requestOptions: err.requestOptions,\r\n'
                '          error: ErrorDeRed(),\r\n'
                '          type: err.type,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '    }\r\n'
                '    final cuerpo = respuesta.data;\r\n'
                '    final mapa = cuerpo is Map<String, dynamic>\r\n'
                '        ? cuerpo\r\n'
                '        : const <String, dynamic>{};\r\n'
                '    final traza =\r\n'
                '        (mapa[\'trazaId\'] as String?) ??\r\n'
                '        (err.requestOptions.headers[\'x-request-id\'] as String? ?? \'\');\r\n'
                '    handler.reject(\r\n'
                '      DioException(\r\n'
                '        requestOptions: err.requestOptions,\r\n'
                '        response: respuesta,\r\n'
                '        type: err.type,\r\n'
                '        error: ErrorDeApi(\r\n'
                '          codigo: (mapa[\'codigo\'] as String?) ?? \'AP-DESCONOCIDO\',\r\n'
                '          trazaId: traza,\r\n'
                '          estado: respuesta.statusCode ?? 0,\r\n'
                '        ),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'String _traza() {\r\n'
                '  final r = Random();\r\n'
                '  return List.generate(\r\n'
                '    16,\r\n'
                '    (_) => r.nextInt(256).toRadixString(16).padLeft(2, \'0\'),\r\n'
                '  ).join();\r\n'
                '}\r\n'
                '\r\n'
                '/// Desenvuelve el `DioException` para que la pantalla vea `ErrorDeApi` o `ErrorDeRed`.\r\n'
                'Object errorDeDominio(Object e) =>\r\n'
                '    e is DioException && e.error != null ? e.error! : e;\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu10_recargar_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                'import \'../proveedores/idempotencia.dart\';\r\n'
                '\r\n'
                '/// CU-10 · recargar saldo (cash-in). La orden queda `PENDIENTE`: la acreditación la\r\n'
                '/// confirma el proveedor por webhook (flujo 3 del CU); acá solo se crea la orden con\r\n'
                '/// su clave de idempotencia, nunca se ajusta el saldo en memoria.\r\n'
                '///\r\n'
                '/// Un provider por pantalla (no por cuenta, siguiendo `altaProvider` de M1): el\r\n'
                '/// identificador del formulario de idempotencia sí es fijo por cuenta, así que un\r\n'
                '/// reintento (doble toque, reconexión) reusa la MISMA clave y el servidor responde\r\n'
                '/// sin duplicar (`R-BIL-06`, invariante 7).\r\n'
                'class RecargarSaldo extends AsyncNotifier<SalidaRecarga?> {\r\n'
                '  static String formularioId(String cuentaId) => \'cu10-recarga-$cuentaId\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<SalidaRecarga?> build() async => null;\r\n'
                '\r\n'
                '  Future<void> enviar({\r\n'
                '    required String cuentaId,\r\n'
                '    required String monto,\r\n'
                '    required String medio,\r\n'
                '  }) async {\r\n'
                '    if (state.isLoading) return; // el botón ya está bloqueado; refuerzo\r\n'
                '    state = const AsyncLoading<SalidaRecarga?>();\r\n'
                '    final dio = ref.read(dioProvider);\r\n'
                '    final clave = ref\r\n'
                '        .read(idempotenciaProvider.notifier)\r\n'
                '        .claveDe(formularioId(cuentaId));\r\n'
                '    try {\r\n'
                '      final r = await DefaultApi(dio).solicitarRecarga(\r\n'
                '        idempotencyKey: clave,\r\n'
                '        entradaRecarga: EntradaRecarga(\r\n'
                '          cuentaBilleteraId: cuentaId,\r\n'
                '          monto: Dinero(monto: monto, moneda: DineroMonedaEnum.BOB),\r\n'
                '          medio: medio,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '      ref.read(idempotenciaProvider.notifier).cerrar(formularioId(cuentaId));\r\n'
                '      state = AsyncData(r.data);\r\n'
                '    } catch (e) {\r\n'
                '      state = AsyncError(errorDeDominio(e), StackTrace.current);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'final recargarSaldoProvider =\r\n'
                '    AsyncNotifierProvider<RecargarSaldo, SalidaRecarga?>(RecargarSaldo.new);\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu11_retirar_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                'import \'../proveedores/idempotencia.dart\';\r\n'
                '\r\n'
                '/// CU-11 · retirar saldo (cash-out). Primero se retiene el saldo, después se instruye\r\n'
                '/// el pago (lo hace el servidor, fuera de esta transacción): la app solo pide el\r\n'
                '/// retiro con `factorMfa` ya capturado por el puerto de biometría/OTP del carril M1.\r\n'
                'class RetirarSaldo extends AsyncNotifier<SalidaRetiro?> {\r\n'
                '  static String formularioId(String cuentaId) => \'cu11-retiro-$cuentaId\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<SalidaRetiro?> build() async => null;\r\n'
                '\r\n'
                '  Future<void> enviar({\r\n'
                '    required String cuentaId,\r\n'
                '    required String monto,\r\n'
                '    required String instrumentoDestinoId,\r\n'
                '    required String factorMfa,\r\n'
                '  }) async {\r\n'
                '    if (state.isLoading) return;\r\n'
                '    state = const AsyncLoading<SalidaRetiro?>();\r\n'
                '    final dio = ref.read(dioProvider);\r\n'
                '    final clave = ref\r\n'
                '        .read(idempotenciaProvider.notifier)\r\n'
                '        .claveDe(formularioId(cuentaId));\r\n'
                '    try {\r\n'
                '      final r = await DefaultApi(dio).solicitarRetiro(\r\n'
                '        idempotencyKey: clave,\r\n'
                '        entradaRetiro: EntradaRetiro(\r\n'
                '          cuentaBilleteraId: cuentaId,\r\n'
                '          monto: Dinero(monto: monto, moneda: DineroMonedaEnum.BOB),\r\n'
                '          instrumentoDestinoId: instrumentoDestinoId,\r\n'
                '          factorMfa: factorMfa,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '      ref.read(idempotenciaProvider.notifier).cerrar(formularioId(cuentaId));\r\n'
                '      state = AsyncData(r.data);\r\n'
                '    } catch (e) {\r\n'
                '      state = AsyncError(errorDeDominio(e), StackTrace.current);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'final retirarSaldoProvider = AsyncNotifierProvider<RetirarSaldo, SalidaRetiro?>(\r\n'
                '  RetirarSaldo.new,\r\n'
                ');\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu12_transferir_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                'import \'../proveedores/idempotencia.dart\';\r\n'
                '\r\n'
                '/// CU-12 · transferir saldo entre billeteras. El dinero no sale del sistema: cambia\r\n'
                '/// de bolsillo. Igual exige clave de idempotencia (invariante 7): una transferencia\r\n'
                '/// repetida por reintento de red no puede acreditar dos veces al destino.\r\n'
                'class TransferirSaldo extends AsyncNotifier<SalidaTransferencia?> {\r\n'
                '  static String formularioId(String cuentaId) => \'cu12-transferencia-$cuentaId\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<SalidaTransferencia?> build() async => null;\r\n'
                '\r\n'
                '  Future<void> enviar({\r\n'
                '    required String cuentaOrigenId,\r\n'
                '    required String monto,\r\n'
                '    required EntradaTransferenciaDestinoTipoEnum tipoDestino,\r\n'
                '    required String valorDestino,\r\n'
                '    required String concepto,\r\n'
                '  }) async {\r\n'
                '    if (state.isLoading) return;\r\n'
                '    state = const AsyncLoading<SalidaTransferencia?>();\r\n'
                '    final dio = ref.read(dioProvider);\r\n'
                '    final clave = ref\r\n'
                '        .read(idempotenciaProvider.notifier)\r\n'
                '        .claveDe(formularioId(cuentaOrigenId));\r\n'
                '    try {\r\n'
                '      final r = await DefaultApi(dio).transferirSaldo(\r\n'
                '        idempotencyKey: clave,\r\n'
                '        entradaTransferencia: EntradaTransferencia(\r\n'
                '          cuentaOrigenId: cuentaOrigenId,\r\n'
                '          destino: EntradaTransferenciaDestino(\r\n'
                '            tipo: tipoDestino,\r\n'
                '            valor: valorDestino,\r\n'
                '          ),\r\n'
                '          monto: Dinero(monto: monto, moneda: DineroMonedaEnum.BOB),\r\n'
                '          concepto: concepto,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '      ref\r\n'
                '          .read(idempotenciaProvider.notifier)\r\n'
                '          .cerrar(formularioId(cuentaOrigenId));\r\n'
                '      state = AsyncData(r.data);\r\n'
                '    } catch (e) {\r\n'
                '      state = AsyncError(errorDeDominio(e), StackTrace.current);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'final transferirSaldoProvider =\r\n'
                '    AsyncNotifierProvider<TransferirSaldo, SalidaTransferencia?>(\r\n'
                '      TransferirSaldo.new,\r\n'
                '    );\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu13_consultar_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                '\r\n'
                '/// CU-13 · consultar el saldo. Un archivo por caso de uso, sobre el cliente generado:\r\n'
                '/// el tipo `SaldoBilletera` viene de `clientes/dart`, nunca se reescribe (invariante 2).\r\n'
                '///\r\n'
                '/// El saldo **no se cachea entre operaciones**: tras recargar o pagar, la pantalla lo\r\n'
                '/// relee (`ref.invalidate`) en vez de sumar en memoria (regla §0.2b del flujo).\r\n'
                'final saldoProvider = FutureProvider.autoDispose.family<SaldoBilletera, String>(\r\n'
                '  (ref, cuentaId) async {\r\n'
                '    final dio = ref.watch(dioProvider);\r\n'
                '    try {\r\n'
                '      final r = await dio.get<Map<String, dynamic>>(\r\n'
                '        \'/billetera/$cuentaId/saldo\',\r\n'
                '      );\r\n'
                '      return SaldoBilletera.fromJson(r.data!);\r\n'
                '    } catch (e) {\r\n'
                '      throw errorDeDominio(e);\r\n'
                '    }\r\n'
                '  },\r\n'
                ');\r\n'
                '\r\n'
                '/// Qué es «vacío» para el saldo: una cuenta en cero **no es un error**, es una cuenta nueva.\r\n'
                'bool saldoEnCero(SaldoBilletera s) =>\r\n'
                '    s.disponible.monto == \'0.00\' && s.retenido.monto == \'0.00\';\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu15_emitir_extracto.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                '\r\n'
                '/// CU-15 · emitir extracto y certificado de saldo. Es una consulta (sin efecto): no\r\n'
                '/// lleva clave de idempotencia. `hashArchivo` es la evidencia de integridad del PDF\r\n'
                '/// que emite el servidor; la app no genera ningún archivo por su cuenta.\r\n'
                'class ParametrosExtracto {\r\n'
                '  const ParametrosExtracto({\r\n'
                '    required this.cuentaId,\r\n'
                '    required this.desde,\r\n'
                '    required this.hasta,\r\n'
                '  });\r\n'
                '  final String cuentaId;\r\n'
                '  final DateTime desde;\r\n'
                '  final DateTime hasta;\r\n'
                '}\r\n'
                '\r\n'
                'final extractoProvider = FutureProvider.autoDispose\r\n'
                '    .family<SalidaExtracto, ParametrosExtracto>((ref, p) async {\r\n'
                '      final dio = ref.watch(dioProvider);\r\n'
                '      try {\r\n'
                '        final r = await DefaultApi(\r\n'
                '          dio,\r\n'
                '        ).emitirExtracto(cuentaId: p.cuentaId, desde: p.desde, hasta: p.hasta);\r\n'
                '        return r.data!;\r\n'
                '      } catch (e) {\r\n'
                '        throw errorDeDominio(e);\r\n'
                '      }\r\n'
                '    });\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\errores.dart': '/// Traducción de `AP-CU<NN>-<nn>` a lenguaje humano. La app no muestra el mensaje\r\n'
                '/// del backend: el texto que lee una persona es decisión de producto.\r\n'
                'library;\r\n'
                '\r\n'
                'import \'package:aportaya_diseno/errores.dart\';\r\n'
                '\r\n'
                'const Map<String, String> _catalogo = {\r\n'
                '  \'AP-CU01-01\':\r\n'
                '      \'Todavía no podemos abrir tu cuenta: falta verificar tus datos.\',\r\n'
                '  \'AP-CU01-03\': \'Ese celular ya tiene una cuenta. Si es tuyo, iniciá sesión.\',\r\n'
                '  \'AP-CU04-01\': \'El código no coincide. Revisalo e intentá de nuevo.\',\r\n'
                '  \'AP-CU04-04\': \'Demasiados intentos. Esperá una hora y volvé a probar.\',\r\n'
                '  \'AP-CU04-06\': \'Tu segundo factor no está activado. Pedí ayuda a soporte.\',\r\n'
                '  \'AP-CU21-03\': \'No pudimos cobrar. Revisá tu saldo e intentá de nuevo.\',\r\n'
                '  \'AP-VAL-03\': \'Este vale ya se usó. Cada vale se canjea una sola vez.\',\r\n'
                '};\r\n'
                '\r\n'
                'String mensajeDe(String codigo, {int? estado}) {\r\n'
                '  final conocido = _catalogo[codigo];\r\n'
                '  if (conocido != null) return conocido;\r\n'
                '  return switch (estado) {\r\n'
                '    401 => \'Tu sesión venció. Volvé a ingresar.\',\r\n'
                '    403 => \'No tenés acceso a esto.\',\r\n'
                '    404 => \'No encontramos lo que buscabas.\',\r\n'
                '    409 =>\r\n'
                '      \'Esta operación ya se hizo o cambió mientras tanto. Revisá el estado antes de repetirla.\',\r\n'
                '    _ => \'Algo salió mal de nuestro lado. Probá de nuevo en un momento.\',\r\n'
                '  };\r\n'
                '}\r\n'
                '\r\n'
                'class ErrorDeApi implements Exception, ErrorPresentable {\r\n'
                '  ErrorDeApi({\r\n'
                '    required this.codigo,\r\n'
                '    required this.trazaId,\r\n'
                '    required this.estado,\r\n'
                '  }) : mensaje = mensajeDe(codigo, estado: estado);\r\n'
                '\r\n'
                '  final String codigo;\r\n'
                '  @override\r\n'
                '  final String trazaId;\r\n'
                '  final int estado;\r\n'
                '  @override\r\n'
                '  final String mensaje;\r\n'
                '\r\n'
                '  @override\r\n'
                '  bool get sinConexion => false;\r\n'
                '\r\n'
                '  @override\r\n'
                '  String toString() => \'ErrorDeApi($codigo, $estado, traza $trazaId)\';\r\n'
                '}\r\n'
                '\r\n'
                'class ErrorDeRed implements Exception, ErrorPresentable {\r\n'
                '  @override\r\n'
                '  String get mensaje =>\r\n'
                '      \'No hay conexión. Te mostramos lo último que vimos; para operar hace falta señal.\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  String? get trazaId => null;\r\n'
                '\r\n'
                '  @override\r\n'
                '  bool get sinConexion => true;\r\n'
                '\r\n'
                '  @override\r\n'
                '  String toString() => \'ErrorDeRed\';\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\foto_del_puente.dart': 'import \'package:dio/dio.dart\';\r\n'
                '\r\n'
                '/// Pide una foto al puente de cámara de desarrollo (`scripts/camara_del_mac.py`).\r\n'
                '///\r\n'
                '/// Vive acá y no en el adaptador porque **toda la red del cliente sale de\r\n'
                '/// `dominio/`** — lo comprueba `scripts/verificar_frontend.py`. Que el destino sea un\r\n'
                '/// servicio local de desarrollo no lo hace menos red.\r\n'
                '///\r\n'
                '/// Devuelve `null` si el puente no está levantado o si macOS todavía no concedió el\r\n'
                '/// permiso de cámara: quien llama ya tiene su camino alternativo.\r\n'
                'Future<List<int>?> pedirFotoAlPuente(String url, {Dio? cliente}) async {\r\n'
                '  try {\r\n'
                '    final r = await (cliente ?? Dio()).get<List<int>>(\r\n'
                '      url,\r\n'
                '      options: Options(\r\n'
                '        responseType: ResponseType.bytes,\r\n'
                '        // La captura arranca la cámara y descarta los primeros cuadros: tarda.\r\n'
                '        receiveTimeout: const Duration(seconds: 25),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '    final bytes = r.data;\r\n'
                '    return (bytes == null || bytes.isEmpty) ? null : bytes;\r\n'
                '  } on Object {\r\n'
                '    return null;\r\n'
                '  }\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\proteccion_de_pantalla_dinero.dart': 'import \'package:flutter/widgets.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../proveedores/proteccion_pantalla.dart\';\r\n'
                '\r\n'
                '/// Gate propio del carril: `screen_protector` activo en las rutas con saldo. El\r\n'
                '/// puerto ya lo declaró el carril M1 (`dominio/puertos/proteccion_pantalla.dart`);\r\n'
                '/// como `navegacion/` (congelado) no activa nada por ruta todavía, cada pantalla de\r\n'
                '/// billetera que muestra saldo se protege a sí misma al entrar y se libera al salir,\r\n'
                '/// para no dejarla prendida para toda la app.\r\n'
                'mixin ConProteccionDePantalla<T extends ConsumerStatefulWidget>\r\n'
                '    on ConsumerState<T> {\r\n'
                '  late final _proteccion = ref.read(proteccionPantallaProvider);\r\n'
                '\r\n'
                '  @override\r\n'
                '  void initState() {\r\n'
                '    super.initState();\r\n'
                '    WidgetsBinding.instance.addPostFrameCallback((_) {\r\n'
                '      if (mounted) _proteccion.activar();\r\n'
                '    });\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  void dispose() {\r\n'
                '    _proteccion.desactivar();\r\n'
                '    super.dispose();\r\n'
                '  }\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\almacen_de_progreso.dart': 'import \'../tutoriales/modelo.dart\';\r\n'
                '\r\n'
                '/// **El puerto del progreso de los tutoriales.**\r\n'
                '///\r\n'
                '/// El motor no sabe si esto termina en el almacén del teléfono o en una tabla del\r\n'
                '/// servidor: pide leer, guardar y reiniciar, y alguien cumple.\r\n'
                '///\r\n'
                '/// Hoy el adaptador es local (`infraestructura/almacen_de_progreso_seguro.dart`)\r\n'
                '/// porque el backend todavía no tiene dónde guardar esto. El contrato remoto está\r\n'
                '/// documentado en `docs/Frontend/Motor de tutoriales.md` §11; el día que exista, se\r\n'
                '/// escribe otro adaptador y se cambia un `override`. Ninguna pantalla se entera.\r\n'
                'abstract interface class AlmacenDeProgreso {\r\n'
                '  /// Todo el progreso de quien tiene la sesión abierta en este teléfono.\r\n'
                '  Future<List<ProgresoDeTutorial>> leer();\r\n'
                '\r\n'
                '  /// Guarda una fila entera. Idempotente: la misma fila dos veces deja lo mismo.\r\n'
                '  Future<void> guardar(ProgresoDeTutorial progreso);\r\n'
                '\r\n'
                '  /// Borra el progreso de UN tutorial: reiniciar es empezar de cero.\r\n'
                '  Future<void> reiniciar(String tutorialId);\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\almacen_seguro.dart': '/// Lo que el dominio necesita del almacén seguro del dispositivo. La\r\n'
                '/// implementación (Keystore en Android, Keychain en iOS) vive en `infraestructura/`.\r\n'
                'abstract interface class AlmacenSeguro {\r\n'
                '  Future<String?> leer(String clave);\r\n'
                '  Future<void> guardar(String clave, String valor);\r\n'
                '  Future<void> borrar(String clave);\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\avisos_push.dart': '/// Registrar el dispositivo para avisos push y enterarse de qué pantalla abrir al\r\n'
                '/// tocar uno. **La bandeja de `/notificaciones/bandeja` es la fuente de verdad**\r\n'
                '/// (ADR-035): el push es un aviso de que hay algo nuevo, nunca el único lugar donde\r\n'
                '/// vive. La implementación (FCM en Android, APNs en iOS) vive en `infraestructura/`.\r\n'
                'abstract interface class AvisosPush {\r\n'
                '  /// El identificador que el backend usa para mandar avisos a este dispositivo.\r\n'
                '  /// `null` si el dispositivo no pudo registrarse (sin Google Play Services, por\r\n'
                '  /// ejemplo): la app sigue funcionando, solo que sin push — la bandeja alcanza.\r\n'
                '  Future<String?> token();\r\n'
                '\r\n'
                '  /// Avisos entrantes con la app abierta, con la ruta a la que hay que navegar.\r\n'
                '  Stream<String> get toques;\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\biometria.dart': '/// La biometría **confirma, no autentica** (regla 2 del shell, `planes/12`): quien\r\n'
                '/// autentica es el servidor con el token; la huella o el rostro solo desbloquean el\r\n'
                '/// envío de una operación ya armada. La implementación (BiometricPrompt en Android,\r\n'
                '/// LocalAuthentication en iOS) vive en `infraestructura/`.\r\n'
                'abstract interface class Biometria {\r\n'
                '  /// Si el dispositivo tiene sensor y ya hay biometría enrolada.\r\n'
                '  Future<bool> disponible();\r\n'
                '\r\n'
                '  /// Pide confirmación con [motivo] visible en el diálogo nativo (por ejemplo,\r\n'
                '  /// «Confirmá el envío de Bs 150»). `true` si la persona confirmó.\r\n'
                '  Future<bool> confirmar(String motivo);\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\camara.dart': '/// Capturar una imagen para el cotejo documental (CU-01) o el QR de recarga.\r\n'
                '/// **F2 la declara sin usuario todavía** — el primero es F3 — porque los siete\r\n'
                '/// puertos se fijan antes de la Ola F2 y un puerto nuevo en T5 sería un micro-PR al\r\n'
                '/// shell que los tres carriles de pantallas tendrían que rebasar (ficha `F2`).\r\n'
                'abstract interface class Camara {\r\n'
                '  /// La ruta del archivo capturado, o `null` si la persona canceló.\r\n'
                '  Future<String?> capturar({required bool esDocumento});\r\n'
                '\r\n'
                '  /// Una foto que **ya existe** en el teléfono, en vez de sacar una nueva.\r\n'
                '  ///\r\n'
                '  /// No es solo una comodidad: mucha gente ya tiene la foto de su carnet, y hay\r\n'
                '  /// teléfonos con la cámara rota. Y es la única vía en el simulador de iOS, que no\r\n'
                '  /// tiene cámara ni puede usar la del Mac —limitación de Apple, no del código—,\r\n'
                '  /// así que sin esto el flujo entero no se puede probar sin un teléfono real.\r\n'
                '  Future<String?> elegirDeLasFotos();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\conectividad.dart': '/// Estar «conectado a wifi» no es tener internet: el adaptador sondea el gateway.\r\n'
                'abstract interface class Conectividad {\r\n'
                '  Stream<bool> get cambios;\r\n'
                '  Future<bool> hayConexion();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\haptica.dart': '/// Retroalimentación táctil corta, para confirmar un toque de dinero o marcar un\r\n'
                '/// error de validación sin depender solo de la vista (accesibilidad).\r\n'
                'abstract interface class Haptica {\r\n'
                '  Future<void> exito();\r\n'
                '  Future<void> error();\r\n'
                '  Future<void> toqueLigero();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\proteccion_pantalla.dart': '/// Bloquear captura y grabación de pantalla en las vistas con saldo o datos\r\n'
                '/// personales (regla 3 del shell). `go_router` lo activa y desactiva por ruta al\r\n'
                '/// entrar y salir — nunca queda prendido para toda la app.\r\n'
                'abstract interface class ProteccionPantalla {\r\n'
                '  Future<void> activar();\r\n'
                '  Future<void> desactivar();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\avance.dart': '/// **Las cuentas del avance, sin estado y sin IO.** Todo lo que el centro de ayuda\r\n'
                '/// muestra sale de acá, así se prueba con listas literales y sin levantar la app.\r\n'
                'library;\r\n'
                '\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// El progreso recién nacido de un tutorial que alguien acaba de abrir.\r\n'
                'ProgresoDeTutorial progresoInicial(\r\n'
                '  TutorialDefinicion t,\r\n'
                '  DateTime ahora, {\r\n'
                '  int repeticiones = 0,\r\n'
                '}) => ProgresoDeTutorial(\r\n'
                '  tutorialId: t.id,\r\n'
                '  version: t.version,\r\n'
                '  estado: EstadoDeProgreso.enProgreso,\r\n'
                '  pasoId: t.pasos.isEmpty ? null : t.pasos.first.id,\r\n'
                '  indice: 0,\r\n'
                '  iniciadoEn: ahora,\r\n'
                '  ultimaInteraccion: ahora,\r\n'
                '  repeticiones: repeticiones,\r\n'
                ');\r\n'
                '\r\n'
                '/// Mueve el progreso a un paso. No decide nada: eso es del motor.\r\n'
                'ProgresoDeTutorial enPaso(\r\n'
                '  ProgresoDeTutorial previo,\r\n'
                '  int indice,\r\n'
                '  String? pasoId,\r\n'
                '  DateTime ahora,\r\n'
                ') => previo.copiaCon(\r\n'
                '  estado: EstadoDeProgreso.enProgreso,\r\n'
                '  indice: indice,\r\n'
                '  pasoId: pasoId,\r\n'
                '  ultimaInteraccion: ahora,\r\n'
                ');\r\n'
                '\r\n'
                '/// Cierra el progreso con un desenlace. Completar suma una repetición; omitir no.\r\n'
                'ProgresoDeTutorial cerrado(\r\n'
                '  ProgresoDeTutorial previo,\r\n'
                '  EstadoDeProgreso estado,\r\n'
                '  DateTime ahora,\r\n'
                ') => previo.copiaCon(\r\n'
                '  estado: estado,\r\n'
                '  terminadoEn: ahora,\r\n'
                '  ultimaInteraccion: ahora,\r\n'
                '  repeticiones: estado == EstadoDeProgreso.completado\r\n'
                '      ? previo.repeticiones + 1\r\n'
                '      : previo.repeticiones,\r\n'
                ');\r\n'
                '\r\n'
                '/// **Cambió la versión del tutorial: lo que había ya no vale.**\r\n'
                '///\r\n'
                '/// El progreso viejo no se borra —se sigue sabiendo que esa persona lo hizo, y las\r\n'
                '/// repeticiones viajan— pero deja de contar como completado y el tutorial vuelve a\r\n'
                '/// ofrecerse. Es la estrategia conservadora a propósito: mostrar de más molesta;\r\n'
                '/// esconder un cambio que importa deja a alguien usando instrucciones viejas.\r\n'
                'bool vigente(ProgresoDeTutorial? p, TutorialDefinicion t) =>\r\n'
                '    p != null && p.version == t.version;\r\n'
                '\r\n'
                '/// El estado con el que se pinta la tarjeta, ya con la versión tenida en cuenta.\r\n'
                'EstadoDeProgreso estadoDe(ProgresoDeTutorial? p, TutorialDefinicion t) =>\r\n'
                '    p == null || !vigente(p, t) ? EstadoDeProgreso.pendiente : p.estado;\r\n'
                '\r\n'
                '/// Se continúa lo que quedó a medias en la versión que está corriendo, **lo haya\r\n'
                '/// dejado a medias o lo haya abandonado a propósito**.\r\n'
                '///\r\n'
                '/// Omitido también cuenta: al salir se promete «guardamos por qué paso ibas». Si\r\n'
                '/// después la tarjeta solo ofreciera empezar de cero, esa promesa sería mentira.\r\n'
                'bool sePuedeContinuar(ProgresoDeTutorial? p, TutorialDefinicion t) {\r\n'
                '  final estado = estadoDe(p, t);\r\n'
                '  return (estado == EstadoDeProgreso.enProgreso ||\r\n'
                '          estado == EstadoDeProgreso.omitido) &&\r\n'
                '      (p?.indice ?? 0) > 0;\r\n'
                '}\r\n'
                '\r\n'
                '/// Cuánto recorrió esta persona, de 0 a 1.\r\n'
                'double fraccionDe(ProgresoDeTutorial? p, TutorialDefinicion t) {\r\n'
                '  final estado = estadoDe(p, t);\r\n'
                '  if (estado == EstadoDeProgreso.completado) return 1;\r\n'
                '  if (t.pasos.isEmpty || p == null || estado == EstadoDeProgreso.pendiente) {\r\n'
                '    return 0;\r\n'
                '  }\r\n'
                '  return (p.indice / t.pasos.length).clamp(0, 1).toDouble();\r\n'
                '}\r\n'
                '\r\n'
                '/// Tutoriales completados sobre tutoriales disponibles. Lo omitido NO cuenta como\r\n'
                '/// hecho —omitir es «esto no lo necesito», no «ya lo sé»— pero tampoco se esconde.\r\n'
                'double avanceGeneral(\r\n'
                '  List<TutorialDefinicion> tutoriales,\r\n'
                '  Map<String, ProgresoDeTutorial> progresos,\r\n'
                ') {\r\n'
                '  if (tutoriales.isEmpty) return 0;\r\n'
                '  final hechos = tutoriales\r\n'
                '      .where((t) => estadoDe(progresos[t.id], t) == EstadoDeProgreso.completado)\r\n'
                '      .length;\r\n'
                '  return hechos / tutoriales.length;\r\n'
                '}\r\n'
                '\r\n'
                '/// Los requisitos que todavía le faltan a quien mira esta tarjeta.\r\n'
                'List<TutorialDefinicion> requisitosPendientes(\r\n'
                '  TutorialDefinicion t,\r\n'
                '  Map<String, TutorialDefinicion> porId,\r\n'
                '  Map<String, ProgresoDeTutorial> progresos,\r\n'
                ') => t.requisitos\r\n'
                '    .map((id) => porId[id])\r\n'
                '    .whereType<TutorialDefinicion>()\r\n'
                '    .where((r) => estadoDe(progresos[r.id], r) != EstadoDeProgreso.completado)\r\n'
                '    .toList();\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\escribano.dart': 'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../../proveedores/tutoriales.dart\';\r\n'
                'import \'avance.dart\';\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// **Quien escribe el avance, para que el motor no se ocupe de eso.**\r\n'
                '///\r\n'
                '/// Guardar no puede demorar el paso siguiente: si el almacén falla, el tutorial sigue y\r\n'
                '/// lo único que se pierde es poder retomarlo después. Por eso hay dos formas de guardar\r\n'
                '/// —esperando y sin esperar— y ninguna lanza hacia el recorrido.\r\n'
                'class EscribanoDeProgreso {\r\n'
                '  const EscribanoDeProgreso(this._ref);\r\n'
                '\r\n'
                '  final Ref _ref;\r\n'
                '\r\n'
                '  /// La fila de este tutorial, o una recién nacida si todavía no hay ninguna. Un\r\n'
                '  /// guardado anterior que falló no puede hacer que terminar un tutorial no cuente.\r\n'
                '  ProgresoDeTutorial de(TutorialDefinicion tutorial, DateTime ahora) =>\r\n'
                '      _ref.read(progresoDeTutorialesProvider)[tutorial.id] ??\r\n'
                '      progresoInicial(tutorial, ahora);\r\n'
                '\r\n'
                '  Future<void> guardar(ProgresoDeTutorial progreso) =>\r\n'
                '      _ref.read(progresoDeTutorialesProvider.notifier).guardar(progreso);\r\n'
                '\r\n'
                '  void sinEsperar(ProgresoDeTutorial progreso) => guardar(progreso).ignore();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\estado_del_motor.dart': 'import \'dart:ui\' show Rect;\r\n'
                '\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// Lo que se ve de un recorrido en curso. Es inmutable: el motor publica estados\r\n'
                '/// nuevos, nadie muta el de adentro.\r\n'
                'class EstadoDelMotor {\r\n'
                '  const EstadoDelMotor({\r\n'
                '    this.tutorial,\r\n'
                '    this.indice = 0,\r\n'
                '    this.recuadro,\r\n'
                '    this.problema,\r\n'
                '    this.accionCumplida = false,\r\n'
                '    this.confirmandoSalida = false,\r\n'
                '  });\r\n'
                '\r\n'
                '  /// `null` es «no hay ningún tutorial corriendo», que es el estado normal.\r\n'
                '  final TutorialDefinicion? tutorial;\r\n'
                '  final int indice;\r\n'
                '\r\n'
                '  /// Dónde está el elemento resaltado. `null` es «todavía no» o «no está».\r\n'
                '  final Rect? recuadro;\r\n'
                '\r\n'
                '  /// Lo que impide seguir, en palabras. Se muestra en el globo, con reintento.\r\n'
                '  final String? problema;\r\n'
                '  final bool accionCumplida;\r\n'
                '  final bool confirmandoSalida;\r\n'
                '\r\n'
                '  bool get activo => tutorial != null;\r\n'
                '  PasoDeTutorial? get paso =>\r\n'
                '      tutorial == null || indice >= tutorial!.pasos.length\r\n'
                '      ? null\r\n'
                '      : tutorial!.pasos[indice];\r\n'
                '  int get total => tutorial?.pasos.length ?? 0;\r\n'
                '  bool get esUltimo => total > 0 && indice >= total - 1;\r\n'
                '\r\n'
                '  /// Se puede avanzar si el paso no pedía nada o si ya se hizo lo que pedía.\r\n'
                '  bool get puedeAvanzar =>\r\n'
                '      paso == null ||\r\n'
                '      paso!.accion.tipo == TipoDeAccion.ninguna ||\r\n'
                '      accionCumplida;\r\n'
                '\r\n'
                '  static const apagado = EstadoDelMotor();\r\n'
                '\r\n'
                '  EstadoDelMotor copiaCon({\r\n'
                '    TutorialDefinicion? tutorial,\r\n'
                '    int? indice,\r\n'
                '    Rect? recuadro,\r\n'
                '    String? problema,\r\n'
                '    bool? accionCumplida,\r\n'
                '    bool? confirmandoSalida,\r\n'
                '    bool limpiarRecuadro = false,\r\n'
                '    bool limpiarProblema = false,\r\n'
                '  }) => EstadoDelMotor(\r\n'
                '    tutorial: tutorial ?? this.tutorial,\r\n'
                '    indice: indice ?? this.indice,\r\n'
                '    recuadro: limpiarRecuadro ? null : (recuadro ?? this.recuadro),\r\n'
                '    problema: limpiarProblema ? null : (problema ?? this.problema),\r\n'
                '    accionCumplida: accionCumplida ?? this.accionCumplida,\r\n'
                '    confirmandoSalida: confirmandoSalida ?? this.confirmandoSalida,\r\n'
                '  );\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\modelo.dart': '/// **El contrato del motor de tutoriales de la app.** Solo datos: ni un `import` de\r\n'
                '/// Flutter, ni una línea de red. Un tutorial se declara en\r\n'
                '/// `pantallas/soporte/catalogo/` y el motor no conoce a ninguno en particular.\r\n'
                '///\r\n'
                '/// Es el mismo modelo que el del backoffice (`apps/backoffice/src/app/nucleo/\r\n'
                '/// tutoriales/tipos.ts`), en Dart: dos productos, un solo concepto de «tutorial».\r\n'
                'library;\r\n'
                '\r\n'
                '/// Lo que alguien puede hacer en la app, y que decide qué tutoriales le sirven.\r\n'
                '///\r\n'
                '/// **No es un permiso**: los permisos los aplica el servidor en cada llamada. Esto solo\r\n'
                '/// decide qué se OFRECE. Un tutorial jamás abre nada que la app no abriera igual.\r\n'
                'enum Capacidad {\r\n'
                '  /// Hay sesión abierta en este teléfono.\r\n'
                '  sesion,\r\n'
                '\r\n'
                '  /// Esta persona está habilitada para organizar un grupo (CU-90).\r\n'
                '  organizador,\r\n'
                '}\r\n'
                '\r\n'
                '/// Dónde se planta el globo respecto del elemento resaltado.\r\n'
                'enum PosicionDeGlobo { arriba, abajo, centro }\r\n'
                '\r\n'
                '/// Cuánto pide el tutorial de quien lo hace. Ordena el catálogo, no bloquea nada.\r\n'
                'enum Dificultad { inicial, intermedio, avanzado }\r\n'
                '\r\n'
                '/// Qué se espera que haga la persona antes de poder seguir.\r\n'
                '///\r\n'
                '/// **Ninguna acción escribe en el servidor.** El motor mira lo que ya pasa en la\r\n'
                '/// pantalla; nunca toca por su cuenta. Un tutorial no aporta, no paga y no retira.\r\n'
                'enum TipoDeAccion {\r\n'
                '  /// Se lee y se sigue con «Siguiente». Es lo normal.\r\n'
                '  ninguna,\r\n'
                '\r\n'
                '  /// Hay que tocar el elemento resaltado.\r\n'
                '  toque,\r\n'
                '\r\n'
                '  /// Hay que llegar a una ruta.\r\n'
                '  navegar,\r\n'
                '\r\n'
                '  /// Hay que hacer que algo aparezca (una hoja, un panel, una fila).\r\n'
                '  aparezca,\r\n'
                '}\r\n'
                '\r\n'
                '/// La acción esperada, con el dato que necesite según su tipo.\r\n'
                'class AccionEsperada {\r\n'
                '  const AccionEsperada.ninguna()\r\n'
                '    : tipo = TipoDeAccion.ninguna,\r\n'
                '      ruta = null,\r\n'
                '      ancla = null;\r\n'
                '  const AccionEsperada.toque({this.ancla})\r\n'
                '    : tipo = TipoDeAccion.toque,\r\n'
                '      ruta = null;\r\n'
                '  const AccionEsperada.navegar(String this.ruta)\r\n'
                '    : tipo = TipoDeAccion.navegar,\r\n'
                '      ancla = null;\r\n'
                '  const AccionEsperada.aparezca(String this.ancla)\r\n'
                '    : tipo = TipoDeAccion.aparezca,\r\n'
                '      ruta = null;\r\n'
                '\r\n'
                '  final TipoDeAccion tipo;\r\n'
                '  final String? ruta;\r\n'
                '  final String? ancla;\r\n'
                '}\r\n'
                '\r\n'
                '/// Un paso: una sola idea, un solo elemento, una sola acción.\r\n'
                'class PasoDeTutorial {\r\n'
                '  const PasoDeTutorial({\r\n'
                '    required this.id,\r\n'
                '    required this.titulo,\r\n'
                '    required this.descripcion,\r\n'
                '    this.ancla,\r\n'
                '    this.posicion = PosicionDeGlobo.abajo,\r\n'
                '    this.ruta,\r\n'
                '    this.orden,\r\n'
                '    this.accion = const AccionEsperada.ninguna(),\r\n'
                '    this.ayudaSiFalla,\r\n'
                '    this.espera = const Duration(seconds: 4),\r\n'
                '    this.bloquea = false,\r\n'
                '  });\r\n'
                '\r\n'
                '  final String id;\r\n'
                '  final String titulo;\r\n'
                '  final String descripcion;\r\n'
                '\r\n'
                '  /// El id de la `MarcaDeTutorial` que envuelve al elemento. Sin ancla, el globo va\r\n'
                '  /// centrado y habla de la pantalla entera.\r\n'
                '  final String? ancla;\r\n'
                '  final PosicionDeGlobo posicion;\r\n'
                '\r\n'
                '  /// La ruta donde este paso tiene sentido. El motor navega si hace falta.\r\n'
                '  final String? ruta;\r\n'
                '\r\n'
                '  /// Solo para validar el orden declarado; el orden real es el de la lista.\r\n'
                '  final int? orden;\r\n'
                '  final AccionEsperada accion;\r\n'
                '\r\n'
                '  /// Qué decirle a quien se quedó trabado.\r\n'
                '  final String? ayudaSiFalla;\r\n'
                '\r\n'
                '  /// Cuánto esperar a un elemento que llega después de una petición.\r\n'
                '  final Duration espera;\r\n'
                '\r\n'
                '  /// Con `true` no se puede tocar la app detrás del velo. Por omisión, sí se puede:\r\n'
                '  /// un paso que dice «tocá este botón» tiene que dejar tocarlo.\r\n'
                '  final bool bloquea;\r\n'
                '}\r\n'
                '\r\n'
                '/// Un tutorial completo. Es una constante, no una clase con lógica.\r\n'
                'class TutorialDefinicion {\r\n'
                '  const TutorialDefinicion({\r\n'
                '    required this.id,\r\n'
                '    required this.version,\r\n'
                '    required this.titulo,\r\n'
                '    required this.descripcion,\r\n'
                '    required this.categoria,\r\n'
                '    required this.dificultad,\r\n'
                '    required this.pasos,\r\n'
                '    this.ruta,\r\n'
                '    this.minutos,\r\n'
                '    this.obligatorio = false,\r\n'
                '    this.requisitos = const [],\r\n'
                '    this.siguiente,\r\n'
                '    this.requiere = const {},\r\n'
                '  });\r\n'
                '\r\n'
                '  final String id;\r\n'
                '\r\n'
                '  /// Cambiarla es decir «esto ya no es el mismo tutorial» (ver `avance.dart`).\r\n'
                '  final String version;\r\n'
                '  final String titulo;\r\n'
                '  final String descripcion;\r\n'
                '  final String categoria;\r\n'
                '  final Dificultad dificultad;\r\n'
                '  final List<PasoDeTutorial> pasos;\r\n'
                '\r\n'
                '  /// Dónde empieza; también enciende el acceso contextual de esa pantalla.\r\n'
                '  final String? ruta;\r\n'
                '  final int? minutos;\r\n'
                '  final bool obligatorio;\r\n'
                '\r\n'
                '  /// Ids de tutoriales que conviene haber hecho antes. Avisan, no bloquean.\r\n'
                '  final List<String> requisitos;\r\n'
                '\r\n'
                '  /// El que se ofrece al terminar este.\r\n'
                '  final String? siguiente;\r\n'
                '\r\n'
                '  /// Qué tiene que poder hacer alguien para que este tutorial le sirva. Un tutorial\r\n'
                '  /// que enseña a crear un grupo no se le ofrece a quien no puede organizar: sería\r\n'
                '  /// enseñarle a usar un botón que no va a ver.\r\n'
                '  final Set<Capacidad> requiere;\r\n'
                '}\r\n'
                '\r\n'
                '/// Qué le pasó a una persona con un tutorial.\r\n'
                'enum EstadoDeProgreso { pendiente, enProgreso, completado, omitido }\r\n'
                '\r\n'
                '/// La fila de progreso. Es lo que viaja al almacén, local hoy y remoto mañana.\r\n'
                'class ProgresoDeTutorial {\r\n'
                '  const ProgresoDeTutorial({\r\n'
                '    required this.tutorialId,\r\n'
                '    required this.version,\r\n'
                '    required this.estado,\r\n'
                '    required this.indice,\r\n'
                '    required this.iniciadoEn,\r\n'
                '    required this.ultimaInteraccion,\r\n'
                '    this.pasoId,\r\n'
                '    this.terminadoEn,\r\n'
                '    this.repeticiones = 0,\r\n'
                '  });\r\n'
                '\r\n'
                '  final String tutorialId;\r\n'
                '  final String version;\r\n'
                '  final EstadoDeProgreso estado;\r\n'
                '  final String? pasoId;\r\n'
                '  final int indice;\r\n'
                '  final DateTime iniciadoEn;\r\n'
                '  final DateTime? terminadoEn;\r\n'
                '  final DateTime ultimaInteraccion;\r\n'
                '  final int repeticiones;\r\n'
                '\r\n'
                '  ProgresoDeTutorial copiaCon({\r\n'
                '    EstadoDeProgreso? estado,\r\n'
                '    String? pasoId,\r\n'
                '    int? indice,\r\n'
                '    DateTime? terminadoEn,\r\n'
                '    DateTime? ultimaInteraccion,\r\n'
                '    int? repeticiones,\r\n'
                '  }) => ProgresoDeTutorial(\r\n'
                '    tutorialId: tutorialId,\r\n'
                '    version: version,\r\n'
                '    estado: estado ?? this.estado,\r\n'
                '    pasoId: pasoId ?? this.pasoId,\r\n'
                '    indice: indice ?? this.indice,\r\n'
                '    iniciadoEn: iniciadoEn,\r\n'
                '    terminadoEn: terminadoEn ?? this.terminadoEn,\r\n'
                '    ultimaInteraccion: ultimaInteraccion ?? this.ultimaInteraccion,\r\n'
                '    repeticiones: repeticiones ?? this.repeticiones,\r\n'
                '  );\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\motor.dart': 'import \'package:aportaya_diseno/moviles/anclas_de_tutorial.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../../proveedores/tutoriales.dart\';\r\n'
                'import \'avance.dart\';\r\n'
                'import \'escribano.dart\';\r\n'
                'import \'estado_del_motor.dart\';\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// **El motor: dónde estoy, cómo sigo y qué hago si el paso no se puede dar.**\r\n'
                '///\r\n'
                '/// Es una máquina de estados; no dibuja nada. El velo, el globo y los gestos viven en\r\n'
                '/// `pantallas/soporte/capa_de_tutorial.dart`, y el catálogo en `pantallas/soporte/\r\n'
                '/// catalogo/`. Entre los tres no hay un solo `if` con el id de un tutorial concreto.\r\n'
                '///\r\n'
                '/// Tres reglas que no se negocian:\r\n'
                '/// 1. **No se traba.** Un objetivo que no aparece es un problema declarado con\r\n'
                '///    reintento y salida, nunca una pantalla congelada.\r\n'
                '/// 2. **No opera.** No toca botones, no envía formularios, no confirma nada.\r\n'
                '/// 3. **No inventa permisos.** Los tutoriales ya vienen filtrados por capacidad.\r\n'
                'class MotorDeTutoriales extends Notifier<EstadoDelMotor> {\r\n'
                '  late final _escribano = EscribanoDeProgreso(ref);\r\n'
                '  AnclasDeTutorial? _anclas;\r\n'
                '  void Function(String ruta)? _navegar;\r\n'
                '  String Function()? _ubicacion;\r\n'
                '\r\n'
                '  @override\r\n'
                '  EstadoDelMotor build() => EstadoDelMotor.apagado;\r\n'
                '\r\n'
                '  /// La capa visual le presta al motor lo que solo existe con un `BuildContext`: el\r\n'
                '  /// registro de anclas y la navegación. Se llama una vez, al montarse la capa.\r\n'
                '  void conectar({\r\n'
                '    required AnclasDeTutorial anclas,\r\n'
                '    required void Function(String ruta) navegar,\r\n'
                '    required String Function() ubicacion,\r\n'
                '  }) {\r\n'
                '    _anclas = anclas;\r\n'
                '    _navegar = navegar;\r\n'
                '    _ubicacion = ubicacion;\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Empieza de cero. Si ya se había hecho, las repeticiones viajan.\r\n'
                '  Future<void> iniciar(String tutorialId, {int repeticiones = 0}) async {\r\n'
                '    final tutorial = ref.read(registroDeTutorialesProvider).buscar(tutorialId);\r\n'
                '    if (tutorial == null || tutorial.pasos.isEmpty) return;\r\n'
                '    state = EstadoDelMotor(tutorial: tutorial);\r\n'
                '    await _escribano.guardar(\r\n'
                '      progresoInicial(tutorial, DateTime.now(), repeticiones: repeticiones),\r\n'
                '    );\r\n'
                '    await _ir(0);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Retoma donde quedó. Un índice fuera de rango vuelve al último paso posible.\r\n'
                '  Future<void> continuar(String tutorialId, ProgresoDeTutorial progreso) async {\r\n'
                '    final tutorial = ref.read(registroDeTutorialesProvider).buscar(tutorialId);\r\n'
                '    if (tutorial == null || tutorial.pasos.isEmpty) return;\r\n'
                '    state = EstadoDelMotor(tutorial: tutorial);\r\n'
                '    await _ir(progreso.indice.clamp(0, tutorial.pasos.length - 1));\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> avanzar() async {\r\n'
                '    if (!state.puedeAvanzar) {\r\n'
                '      state = state.copiaCon(\r\n'
                '        problema:\r\n'
                '            state.paso?.ayudaSiFalla ?? \'Hacé lo que pide el paso para seguir.\',\r\n'
                '      );\r\n'
                '      return;\r\n'
                '    }\r\n'
                '    if (state.esUltimo) return _terminar();\r\n'
                '    await _ir(state.indice + 1);\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> retroceder() async {\r\n'
                '    if (state.indice == 0) return;\r\n'
                '    await _ir(state.indice - 1);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Reintenta el paso actual: vuelve a buscar el objetivo desde cero.\r\n'
                '  Future<void> reintentar() => _ir(state.indice);\r\n'
                '\r\n'
                '  /// La acción que el paso pedía ya está hecha.\r\n'
                '  void marcarAccionCumplida() {\r\n'
                '    if (state.activo) state = state.copiaCon(accionCumplida: true);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Pide confirmación si va por la mitad; si recién empezó, cierra sin preguntar.\r\n'
                '  void pedirSalida() {\r\n'
                '    if (state.indice == 0 || state.esUltimo) {\r\n'
                '      omitir();\r\n'
                '      return;\r\n'
                '    }\r\n'
                '    state = state.copiaCon(confirmandoSalida: true);\r\n'
                '  }\r\n'
                '\r\n'
                '  void seguirEnElTutorial() {\r\n'
                '    state = state.copiaCon(confirmandoSalida: false);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Abandona: queda omitido, con el paso donde se fue. Se puede repetir después.\r\n'
                '  void omitir() {\r\n'
                '    final tutorial = state.tutorial;\r\n'
                '    if (tutorial != null) {\r\n'
                '      final ahora = DateTime.now();\r\n'
                '      _escribano.sinEsperar(\r\n'
                '        cerrado(\r\n'
                '          enPaso(\r\n'
                '            _escribano.de(tutorial, ahora),\r\n'
                '            state.indice,\r\n'
                '            state.paso?.id,\r\n'
                '            ahora,\r\n'
                '          ),\r\n'
                '          EstadoDeProgreso.omitido,\r\n'
                '          ahora,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '    }\r\n'
                '    state = EstadoDelMotor.apagado;\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> _terminar() async {\r\n'
                '    final tutorial = state.tutorial;\r\n'
                '    state = EstadoDelMotor.apagado;\r\n'
                '    if (tutorial == null) return;\r\n'
                '    final ahora = DateTime.now();\r\n'
                '    final hecho = _escribano\r\n'
                '        .de(tutorial, ahora)\r\n'
                '        .copiaCon(indice: tutorial.pasos.length);\r\n'
                '    await _escribano.guardar(\r\n'
                '      cerrado(hecho, EstadoDeProgreso.completado, ahora),\r\n'
                '    );\r\n'
                '  }\r\n'
                '\r\n'
                '  /// El corazón: navegar si el paso vive en otra pantalla, esperar al elemento y dejar\r\n'
                '  /// todo listo. Cada regreso temprano deja un problema declarado, nunca silencio.\r\n'
                '  Future<void> _ir(int indice) async {\r\n'
                '    final tutorial = state.tutorial;\r\n'
                '    if (tutorial == null || indice >= tutorial.pasos.length) return;\r\n'
                '    final paso = tutorial.pasos[indice];\r\n'
                '    state = state.copiaCon(\r\n'
                '      indice: indice,\r\n'
                '      accionCumplida: false,\r\n'
                '      limpiarProblema: true,\r\n'
                '      limpiarRecuadro: true,\r\n'
                '    );\r\n'
                '\r\n'
                '    _escribano.sinEsperar(\r\n'
                '      enPaso(\r\n'
                '        _escribano.de(tutorial, DateTime.now()),\r\n'
                '        indice,\r\n'
                '        paso.id,\r\n'
                '        DateTime.now(),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '\r\n'
                '    // El primer paso hereda la ruta del tutorial: `ruta` de la definición es «dónde\r\n'
                '    // empieza». Sin esto, abrir desde Ayuda un tutorial de otra pantalla dejaba a la\r\n'
                '    // persona en Ayuda, con el primer ancla imposible de encontrar.\r\n'
                '    if (!_llegarA(paso.ruta ?? (indice == 0 ? tutorial.ruta : null))) return;\r\n'
                '    if (paso.ancla == null) return;\r\n'
                '    final recuadro = await _anclas?.esperar(paso.ancla!, paso.espera);\r\n'
                '    if (!state.activo || state.indice != indice) return;\r\n'
                '    if (recuadro == null) {\r\n'
                '      state = state.copiaCon(\r\n'
                '        problema: \'No encontramos «${paso.ancla}» en esta pantalla.\',\r\n'
                '      );\r\n'
                '      return;\r\n'
                '    }\r\n'
                '    state = state.copiaCon(recuadro: recuadro);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Lleva a esa ruta si hace falta. `false` si no se pudo.\r\n'
                '  bool _llegarA(String? ruta) {\r\n'
                '    if (ruta == null) return true;\r\n'
                '    final actual = _ubicacion?.call() ?? \'\';\r\n'
                '    if (actual == ruta || actual.startsWith(\'$ruta/\')) return true;\r\n'
                '    if (_navegar == null) {\r\n'
                '      state = state.copiaCon(problema: \'No pudimos abrir «$ruta» desde acá.\');\r\n'
                '      return false;\r\n'
                '    }\r\n'
                '    _navegar!(ruta);\r\n'
                '    return true;\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Vuelve a medir el elemento del paso actual: la pantalla se desplazó o cambió de\r\n'
                '  /// tamaño y el agujero tiene que seguirla.\r\n'
                '  void medirDeNuevo() {\r\n'
                '    final ancla = state.paso?.ancla;\r\n'
                '    if (!state.activo || ancla == null) return;\r\n'
                '    final recuadro = _anclas?.recuadroDe(ancla);\r\n'
                '    if (recuadro != null && recuadro != state.recuadro) {\r\n'
                '      state = state.copiaCon(recuadro: recuadro);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '',
              ...
            }
    Actual: {
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\app.dart': 'import \'dart:async\';\r\n'
                '\r\n'
                'import \'package:flutter/material.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                'import \'package:go_router/go_router.dart\';\r\n'
                '\r\n'
                'import \'dominio/verificacion_contrato.dart\';\r\n'
                'import \'navegacion/rutas.dart\';\r\n'
                'import \'pantallas/soporte/capa_de_tutorial.dart\';\r\n'
                'import \'proveedores/sesion.dart\';\r\n'
                'import \'package:aportaya_diseno/moviles/apertura_de_marca.dart\';\r\n'
                'import \'package:aportaya_diseno/moviles/anclas_de_tutorial.dart\';\r\n'
                'import \'package:aportaya_diseno/moviles/como_en_un_telefono.dart\';\r\n'
                'import \'package:aportaya_diseno/tema.dart\';\r\n'
                'import \'package:aportaya_diseno/tokens/tokens.dart\';\r\n'
                '\r\n'
                'class AppAportaYa extends ConsumerWidget {\r\n'
                '  /// [inicial] nulo significa «la que diga el enrutador». Antes esta clase repetía su\r\n'
                '  /// propia ruta de arranque, y cuando la del enrutador cambió a la portada, esta\r\n'
                '  /// copia siguió mandando a la billetera: la app abría adentro igual. Un valor por\r\n'
                '  /// defecto en dos lugares es un valor por defecto que va a divergir.\r\n'
                '  AppAportaYa({super.key, String? inicial})\r\n'
                '    : _enrutador = inicial == null\r\n'
                '          ? crearEnrutador()\r\n'
                '          : crearEnrutador(inicial: inicial);\r\n'
                '\r\n'
                '  final GoRouter _enrutador;\r\n'
                '\r\n'
                '  @override\r\n'
                '  Widget build(BuildContext context, WidgetRef ref) {\r\n'
                '    // Se dispara al construir el árbol, sin bloquear el primer frame: si el\r\n'
                '    // gateway está caído al iniciar, el arranque no se cuelga esperándolo.\r\n'
                '    ref.watch(verificacionContratoProvider);\r\n'
                '    return MaterialApp.router(\r\n'
                '      title: \'AportaYa\',\r\n'
                '      theme: temaDesde(Tokens.claro, Brightness.light),\r\n'
                '      darkTheme: temaDesde(Tokens.oscuro, Brightness.dark),\r\n'
                '      themeMode: ThemeMode.system,\r\n'
                '      routerConfig: _enrutador,\r\n'
                '      debugShowCheckedModeBanner: false,\r\n'
                '      // El registro de anclas y la capa del tutorial envuelven a TODA la app: un\r\n'
                '      // recorrido cruza pantallas, así que no puede vivir dentro de una. Con el\r\n'
                '      // tutorial apagado, las dos piezas no dibujan ni escuchan nada.\r\n'
                '      builder: (context, child) => ComoEnUnTelefono(\r\n'
                '        child: ProveedorDeAnclas(\r\n'
                '          hijo: CapaDeTutorial(\r\n'
                '            enrutador: _enrutador,\r\n'
                '            hijo: _ConApertura(\r\n'
                '              enrutador: _enrutador,\r\n'
                '              child: _AvisoDeContrato(child: child),\r\n'
                '            ),\r\n'
                '          ),\r\n'
                '        ),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// Una franja angosta arriba, no un diálogo que tapa la pantalla: el aviso de\r\n'
                '/// «actualizá la app» no puede impedir seguir usando lo que ya funciona.\r\n'
                'class _AvisoDeContrato extends ConsumerWidget {\r\n'
                '  const _AvisoDeContrato({required this.child});\r\n'
                '  final Widget? child;\r\n'
                '\r\n'
                '  @override\r\n'
                '  Widget build(BuildContext context, WidgetRef ref) {\r\n'
                '    final estado = ref.watch(verificacionContratoProvider).asData?.value;\r\n'
                '    if (estado != EstadoContrato.desactualizado || child == null) {\r\n'
                '      return child ?? const SizedBox.shrink();\r\n'
                '    }\r\n'
                '    return Column(\r\n'
                '      children: [\r\n'
                '        Material(\r\n'
                '          color: Theme.of(context).colorScheme.errorContainer,\r\n'
                '          child: SafeArea(\r\n'
                '            bottom: false,\r\n'
                '            child: Padding(\r\n'
                '              padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),\r\n'
                '              child: Text(\r\n'
                '                \'Hay una versión nueva de AportaYa. Actualizala para seguir \'\r\n'
                '                \'operando sin problemas.\',\r\n'
                '                style: Theme.of(context).textTheme.bodySmall,\r\n'
                '              ),\r\n'
                '            ),\r\n'
                '          ),\r\n'
                '        ),\r\n'
                '        Expanded(child: child!),\r\n'
                '      ],\r\n'
                '    );\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// La apertura de marca por encima de la app, una sola vez por arranque.\r\n'
                '///\r\n'
                '/// Va acá arriba y no como una ruta: así la app **ya está armada y pidiendo el saldo**\r\n'
                '/// detrás de la animación, en vez de empezar a cargar recién cuando la apertura\r\n'
                '/// termina. La animación tapa el arranque; no lo demora.\r\n'
                '/// También es el momento en que se decide **a dónde entrar**: mientras la marca se\r\n'
                '/// acerca, se lee el almacén seguro. Si hay una sesión guardada, la app abre en la\r\n'
                '/// billetera; si no, se queda en la portada. Es el único lugar donde esa pregunta se\r\n'
                '/// hace, y se hace una sola vez por arranque: preguntarla en cada navegación sería\r\n'
                '/// tocar el llavero del teléfono a cada paso.\r\n'
                'class _ConApertura extends ConsumerStatefulWidget {\r\n'
                '  const _ConApertura({required this.child, required this.enrutador});\r\n'
                '  final Widget child;\r\n'
                '  final GoRouter enrutador;\r\n'
                '\r\n'
                '  @override\r\n'
                '  ConsumerState<_ConApertura> createState() => _ConAperturaState();\r\n'
                '}\r\n'
                '\r\n'
                'class _ConAperturaState extends ConsumerState<_ConApertura> {\r\n'
                '  bool _mostrando = true;\r\n'
                '  Timer? _respaldo;\r\n'
                '\r\n'
                '  @override\r\n'
                '  void initState() {\r\n'
                '    super.initState();\r\n'
                '    _decidirDondeEntrar();\r\n'
                '    // El cinturón de seguridad: si la animación no llamara a su final —un ticker que\r\n'
                '    // no arranca, un frame que no llega—, a los dos segundos la apertura se va igual.\r\n'
                '    // Nadie se queda mirando un logo sin poder entrar a su plata.\r\n'
                '    _respaldo = Timer(const Duration(seconds: 2), _ocultar);\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> _decidirDondeEntrar() async {\r\n'
                '    // Si leer el llavero falla —teléfono con el almacén bloqueado, un permiso raro—,\r\n'
                '    // lo seguro es la portada: se pide ingresar de nuevo. Nunca al revés.\r\n'
                '    String? token;\r\n'
                '    try {\r\n'
                '      token = await ref.read(sesionProvider).tokenDeAcceso();\r\n'
                '    } on Object {\r\n'
                '      token = null;\r\n'
                '    }\r\n'
                '    if (!mounted || token == null || token.isEmpty) return;\r\n'
                '    if (widget.enrutador.state.uri.toString() == \'/portada\') {\r\n'
                '      widget.enrutador.go(\'/billetera/inicio\');\r\n'
                '    }\r\n'
                '  }\r\n'
                '\r\n'
                '  void _ocultar() {\r\n'
                '    _respaldo?.cancel();\r\n'
                '    if (!mounted || !_mostrando) return;\r\n'
                '    // Con «reducir movimiento», la apertura pide cerrarse mientras se está\r\n'
                '    // construyendo el árbol, y ahí `setState` sobre un ancestro es un error de\r\n'
                '    // Flutter. Se difiere al frame siguiente, que además es cuando corresponde.\r\n'
                '    WidgetsBinding.instance.addPostFrameCallback((_) {\r\n'
                '      if (!mounted || !_mostrando) return;\r\n'
                '      setState(() => _mostrando = false);\r\n'
                '    });\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  void dispose() {\r\n'
                '    _respaldo?.cancel();\r\n'
                '    super.dispose();\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  Widget build(BuildContext context) => Stack(\r\n'
                '    children: [\r\n'
                '      widget.child,\r\n'
                '      if (_mostrando)\r\n'
                '        Positioned.fill(child: AperturaDeMarca(alTerminar: _ocultar)),\r\n'
                '    ],\r\n'
                '  );\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\catalogo.dart': 'import \'package:flutter/material.dart\';\r\n'
                '\r\n'
                'import \'package:aportaya_diseno/catalogo/catalogo.dart\';\r\n'
                '\r\n'
                '/// Punto de entrada del catálogo del sistema de diseño en dispositivo:\r\n'
                '///   flutter run -t lib/catalogo.dart\r\n'
                'void main() => runApp(const CatalogoDeDiseno());\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cliente.dart': 'import \'dart:math\';\r\n'
                '\r\n'
                'import \'package:dio/dio.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/errores.dart\';\r\n'
                'import \'../proveedores/sesion.dart\';\r\n'
                '\r\n'
                '/// **Una sola base URL: el gateway.** El prefijo enruta al servicio; la app no\r\n'
                '/// conoce catorce direcciones.\r\n'
                '///\r\n'
                '/// Por omisión apunta al **backend de verdad** en la máquina de desarrollo: NGINX\r\n'
                '/// publica el 80, y el gateway enruta `/api/v1/<prefijo>` al servicio que reservó ese\r\n'
                '/// prefijo. Antes apuntaba a Prism, el simulado, y con eso la app «funcionaba» sin\r\n'
                '/// que existiera un backend detrás: cualquier celular y cualquier contraseña entraban.\r\n'
                '///\r\n'
                '/// El simulado sigue disponible para pruebas de contrato, pero hay que pedirlo:\r\n'
                '/// `--dart-define=API=http://localhost:4010/api/v1`. Desde el emulador de Android la\r\n'
                '/// máquina es `10.0.2.2`; desde un teléfono real, la IP de la red local.\r\n'
                'const String baseDelGateway = String.fromEnvironment(\r\n'
                '  \'API\',\r\n'
                '  defaultValue: \'http://localhost/api/v1\',\r\n'
                ');\r\n'
                '\r\n'
                '/// La única salida a la red de la app. Ningún widget crea un `Dio`.\r\n'
                'final dioProvider = Provider<Dio>((ref) {\r\n'
                '  final dio = Dio(\r\n'
                '    BaseOptions(\r\n'
                '      baseUrl: baseDelGateway,\r\n'
                '      connectTimeout: const Duration(seconds: 8),\r\n'
                '      receiveTimeout: const Duration(seconds: 12),\r\n'
                '      headers: {\'Accept\': \'application/json\'},\r\n'
                '    ),\r\n'
                '  );\r\n'
                '  instalarInterceptores(dio, ref.read(sesionProvider));\r\n'
                '  return dio;\r\n'
                '});\r\n'
                '\r\n'
                '/// Los mismos interceptores en la app y en las pruebas: lo que se prueba es la\r\n'
                '/// traducción de errores y la traza reales, no un doble.\r\n'
                'void instalarInterceptores(Dio dio, Sesion sesion) {\r\n'
                '  // Mismo transporte (adaptador) que `dio` — así las pruebas que instalan un\r\n'
                '  // adaptador simulado en `dio` también cubren el refresco — pero SIN sus\r\n'
                '  // interceptores: `_TrazaYSesion` no debe reentrar sobre su propio refresco.\r\n'
                '  final dioDeRefresco = Dio(BaseOptions(baseUrl: dio.options.baseUrl))\r\n'
                '    ..httpClientAdapter = dio.httpClientAdapter;\r\n'
                '  final refrescador = Refrescador(sesion, dioDeRefresco);\r\n'
                '  dio.interceptors.add(_TrazaYSesion(sesion, dio, refrescador));\r\n'
                '  dio.interceptors.add(_TraduccionDeErrores());\r\n'
                '}\r\n'
                '\r\n'
                '/// Un solo refresco en vuelo por vez: N peticiones que reciben `401` a la vez\r\n'
                '/// comparten el mismo `Future` en vez de disparar un `POST /sesion/refrescar` cada\r\n'
                '/// una. Sale por un `Dio` propio, sin los interceptores de la app: si pasara por\r\n'
                '/// `_TrazaYSesion`, un `401` del propio refresco reentraría a este mismo mecanismo.\r\n'
                'class Refrescador {\r\n'
                '  Refrescador(this._sesion, this._dioSinInterceptores);\r\n'
                '  final Sesion _sesion;\r\n'
                '  final Dio _dioSinInterceptores;\r\n'
                '  Future<bool>? _enVuelo;\r\n'
                '\r\n'
                '  Future<bool> refrescar() => _enVuelo ??= _hacerRefresco().whenComplete(() {\r\n'
                '    _enVuelo = null;\r\n'
                '  });\r\n'
                '\r\n'
                '  Future<bool> _hacerRefresco() async {\r\n'
                '    final refresco = await _sesion.tokenDeRefresco();\r\n'
                '    if (refresco == null) return false;\r\n'
                '    try {\r\n'
                '      final r = await _dioSinInterceptores.post<Map<String, dynamic>>(\r\n'
                '        \'/sesion/refrescar\',\r\n'
                '        data: {\'refresco\': refresco},\r\n'
                '      );\r\n'
                '      final acceso = r.data?[\'acceso\'] as String?;\r\n'
                '      final nuevo = r.data?[\'refresco\'] as String?;\r\n'
                '      if (acceso == null || nuevo == null) return false;\r\n'
                '      await _sesion.guardar(acceso: acceso, refresco: nuevo);\r\n'
                '      return true;\r\n'
                '    } on DioException {\r\n'
                '      return false;\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// `x-request-id` en cada petición, bearer desde el almacén seguro, y **un** refresco\r\n'
                '/// compartido (single-flight, `Refrescador`) con **un** reintento por petición. Si\r\n'
                '/// falla, sesión cerrada, no bucle.\r\n'
                'class _TrazaYSesion extends Interceptor {\r\n'
                '  _TrazaYSesion(this._sesion, this._dio, this._refrescador);\r\n'
                '  final Sesion _sesion;\r\n'
                '  final Dio _dio;\r\n'
                '  final Refrescador _refrescador;\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<void> onRequest(\r\n'
                '    RequestOptions options,\r\n'
                '    RequestInterceptorHandler handler,\r\n'
                '  ) async {\r\n'
                '    options.headers[\'x-request-id\'] ??= _traza();\r\n'
                '    final token = await _sesion.tokenDeAcceso();\r\n'
                '    if (token != null) options.headers[\'Authorization\'] = \'Bearer $token\';\r\n'
                '    handler.next(options);\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<void> onError(\r\n'
                '    DioException err,\r\n'
                '    ErrorInterceptorHandler handler,\r\n'
                '  ) async {\r\n'
                '    final yaReintentada = err.requestOptions.extra[\'reintentada\'] == true;\r\n'
                '    if (err.response?.statusCode != 401 || yaReintentada) {\r\n'
                '      return handler.next(err);\r\n'
                '    }\r\n'
                '    final refrescada = await _refrescador.refrescar();\r\n'
                '    if (!refrescada) {\r\n'
                '      await _sesion.cerrar();\r\n'
                '      return handler.next(err);\r\n'
                '    }\r\n'
                '    final opciones = err.requestOptions..extra[\'reintentada\'] = true;\r\n'
                '    try {\r\n'
                '      handler.resolve(await _dio.fetch(opciones));\r\n'
                '    } on DioException catch (e) {\r\n'
                '      if (e.response?.statusCode == 401) {\r\n'
                '        await _sesion.cerrar();\r\n'
                '      }\r\n'
                '      handler.next(e);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                '/// Un error de red no llegó al backend: no lleva código de seguimiento. Un error de\r\n'
                '/// API llega con `AP-CU<NN>-<nn>` y se traduce por catálogo, nunca se muestra crudo.\r\n'
                'class _TraduccionDeErrores extends Interceptor {\r\n'
                '  @override\r\n'
                '  void onError(DioException err, ErrorInterceptorHandler handler) {\r\n'
                '    final respuesta = err.response;\r\n'
                '    if (respuesta == null) {\r\n'
                '      return handler.reject(\r\n'
                '        DioException(\r\n'
                '          requestOptions: err.requestOptions,\r\n'
                '          error: ErrorDeRed(),\r\n'
                '          type: err.type,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '    }\r\n'
                '    final cuerpo = respuesta.data;\r\n'
                '    final mapa = cuerpo is Map<String, dynamic>\r\n'
                '        ? cuerpo\r\n'
                '        : const <String, dynamic>{};\r\n'
                '    final traza =\r\n'
                '        (mapa[\'trazaId\'] as String?) ??\r\n'
                '        (err.requestOptions.headers[\'x-request-id\'] as String? ?? \'\');\r\n'
                '    handler.reject(\r\n'
                '      DioException(\r\n'
                '        requestOptions: err.requestOptions,\r\n'
                '        response: respuesta,\r\n'
                '        type: err.type,\r\n'
                '        error: ErrorDeApi(\r\n'
                '          codigo: (mapa[\'codigo\'] as String?) ?? \'AP-DESCONOCIDO\',\r\n'
                '          trazaId: traza,\r\n'
                '          estado: respuesta.statusCode ?? 0,\r\n'
                '        ),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'String _traza() {\r\n'
                '  final r = Random();\r\n'
                '  return List.generate(\r\n'
                '    16,\r\n'
                '    (_) => r.nextInt(256).toRadixString(16).padLeft(2, \'0\'),\r\n'
                '  ).join();\r\n'
                '}\r\n'
                '\r\n'
                '/// Desenvuelve el `DioException` para que la pantalla vea `ErrorDeApi` o `ErrorDeRed`.\r\n'
                'Object errorDeDominio(Object e) =>\r\n'
                '    e is DioException && e.error != null ? e.error! : e;\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu10_recargar_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                'import \'../proveedores/idempotencia.dart\';\r\n'
                '\r\n'
                '/// CU-10 · recargar saldo (cash-in). La orden queda `PENDIENTE`: la acreditación la\r\n'
                '/// confirma el proveedor por webhook (flujo 3 del CU); acá solo se crea la orden con\r\n'
                '/// su clave de idempotencia, nunca se ajusta el saldo en memoria.\r\n'
                '///\r\n'
                '/// Un provider por pantalla (no por cuenta, siguiendo `altaProvider` de M1): el\r\n'
                '/// identificador del formulario de idempotencia sí es fijo por cuenta, así que un\r\n'
                '/// reintento (doble toque, reconexión) reusa la MISMA clave y el servidor responde\r\n'
                '/// sin duplicar (`R-BIL-06`, invariante 7).\r\n'
                'class RecargarSaldo extends AsyncNotifier<SalidaRecarga?> {\r\n'
                '  static String formularioId(String cuentaId) => \'cu10-recarga-$cuentaId\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<SalidaRecarga?> build() async => null;\r\n'
                '\r\n'
                '  Future<void> enviar({\r\n'
                '    required String cuentaId,\r\n'
                '    required String monto,\r\n'
                '    required String medio,\r\n'
                '  }) async {\r\n'
                '    if (state.isLoading) return; // el botón ya está bloqueado; refuerzo\r\n'
                '    state = const AsyncLoading<SalidaRecarga?>();\r\n'
                '    final dio = ref.read(dioProvider);\r\n'
                '    final clave = ref\r\n'
                '        .read(idempotenciaProvider.notifier)\r\n'
                '        .claveDe(formularioId(cuentaId));\r\n'
                '    try {\r\n'
                '      final r = await DefaultApi(dio).solicitarRecarga(\r\n'
                '        idempotencyKey: clave,\r\n'
                '        entradaRecarga: EntradaRecarga(\r\n'
                '          cuentaBilleteraId: cuentaId,\r\n'
                '          monto: Dinero(monto: monto, moneda: DineroMonedaEnum.BOB),\r\n'
                '          medio: medio,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '      ref.read(idempotenciaProvider.notifier).cerrar(formularioId(cuentaId));\r\n'
                '      state = AsyncData(r.data);\r\n'
                '    } catch (e) {\r\n'
                '      state = AsyncError(errorDeDominio(e), StackTrace.current);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'final recargarSaldoProvider =\r\n'
                '    AsyncNotifierProvider<RecargarSaldo, SalidaRecarga?>(RecargarSaldo.new);\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu11_retirar_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                'import \'../proveedores/idempotencia.dart\';\r\n'
                '\r\n'
                '/// CU-11 · retirar saldo (cash-out). Primero se retiene el saldo, después se instruye\r\n'
                '/// el pago (lo hace el servidor, fuera de esta transacción): la app solo pide el\r\n'
                '/// retiro con `factorMfa` ya capturado por el puerto de biometría/OTP del carril M1.\r\n'
                'class RetirarSaldo extends AsyncNotifier<SalidaRetiro?> {\r\n'
                '  static String formularioId(String cuentaId) => \'cu11-retiro-$cuentaId\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<SalidaRetiro?> build() async => null;\r\n'
                '\r\n'
                '  Future<void> enviar({\r\n'
                '    required String cuentaId,\r\n'
                '    required String monto,\r\n'
                '    required String instrumentoDestinoId,\r\n'
                '    required String factorMfa,\r\n'
                '  }) async {\r\n'
                '    if (state.isLoading) return;\r\n'
                '    state = const AsyncLoading<SalidaRetiro?>();\r\n'
                '    final dio = ref.read(dioProvider);\r\n'
                '    final clave = ref\r\n'
                '        .read(idempotenciaProvider.notifier)\r\n'
                '        .claveDe(formularioId(cuentaId));\r\n'
                '    try {\r\n'
                '      final r = await DefaultApi(dio).solicitarRetiro(\r\n'
                '        idempotencyKey: clave,\r\n'
                '        entradaRetiro: EntradaRetiro(\r\n'
                '          cuentaBilleteraId: cuentaId,\r\n'
                '          monto: Dinero(monto: monto, moneda: DineroMonedaEnum.BOB),\r\n'
                '          instrumentoDestinoId: instrumentoDestinoId,\r\n'
                '          factorMfa: factorMfa,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '      ref.read(idempotenciaProvider.notifier).cerrar(formularioId(cuentaId));\r\n'
                '      state = AsyncData(r.data);\r\n'
                '    } catch (e) {\r\n'
                '      state = AsyncError(errorDeDominio(e), StackTrace.current);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'final retirarSaldoProvider = AsyncNotifierProvider<RetirarSaldo, SalidaRetiro?>(\r\n'
                '  RetirarSaldo.new,\r\n'
                ');\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu12_transferir_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                'import \'../proveedores/idempotencia.dart\';\r\n'
                '\r\n'
                '/// CU-12 · transferir saldo entre billeteras. El dinero no sale del sistema: cambia\r\n'
                '/// de bolsillo. Igual exige clave de idempotencia (invariante 7): una transferencia\r\n'
                '/// repetida por reintento de red no puede acreditar dos veces al destino.\r\n'
                'class TransferirSaldo extends AsyncNotifier<SalidaTransferencia?> {\r\n'
                '  static String formularioId(String cuentaId) => \'cu12-transferencia-$cuentaId\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  Future<SalidaTransferencia?> build() async => null;\r\n'
                '\r\n'
                '  Future<void> enviar({\r\n'
                '    required String cuentaOrigenId,\r\n'
                '    required String monto,\r\n'
                '    required EntradaTransferenciaDestinoTipoEnum tipoDestino,\r\n'
                '    required String valorDestino,\r\n'
                '    required String concepto,\r\n'
                '  }) async {\r\n'
                '    if (state.isLoading) return;\r\n'
                '    state = const AsyncLoading<SalidaTransferencia?>();\r\n'
                '    final dio = ref.read(dioProvider);\r\n'
                '    final clave = ref\r\n'
                '        .read(idempotenciaProvider.notifier)\r\n'
                '        .claveDe(formularioId(cuentaOrigenId));\r\n'
                '    try {\r\n'
                '      final r = await DefaultApi(dio).transferirSaldo(\r\n'
                '        idempotencyKey: clave,\r\n'
                '        entradaTransferencia: EntradaTransferencia(\r\n'
                '          cuentaOrigenId: cuentaOrigenId,\r\n'
                '          destino: EntradaTransferenciaDestino(\r\n'
                '            tipo: tipoDestino,\r\n'
                '            valor: valorDestino,\r\n'
                '          ),\r\n'
                '          monto: Dinero(monto: monto, moneda: DineroMonedaEnum.BOB),\r\n'
                '          concepto: concepto,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '      ref\r\n'
                '          .read(idempotenciaProvider.notifier)\r\n'
                '          .cerrar(formularioId(cuentaOrigenId));\r\n'
                '      state = AsyncData(r.data);\r\n'
                '    } catch (e) {\r\n'
                '      state = AsyncError(errorDeDominio(e), StackTrace.current);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '\r\n'
                'final transferirSaldoProvider =\r\n'
                '    AsyncNotifierProvider<TransferirSaldo, SalidaTransferencia?>(\r\n'
                '      TransferirSaldo.new,\r\n'
                '    );\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu13_consultar_saldo.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                '\r\n'
                '/// CU-13 · consultar el saldo. Un archivo por caso de uso, sobre el cliente generado:\r\n'
                '/// el tipo `SaldoBilletera` viene de `clientes/dart`, nunca se reescribe (invariante 2).\r\n'
                '///\r\n'
                '/// El saldo **no se cachea entre operaciones**: tras recargar o pagar, la pantalla lo\r\n'
                '/// relee (`ref.invalidate`) en vez de sumar en memoria (regla §0.2b del flujo).\r\n'
                'final saldoProvider = FutureProvider.autoDispose.family<SaldoBilletera, String>(\r\n'
                '  (ref, cuentaId) async {\r\n'
                '    final dio = ref.watch(dioProvider);\r\n'
                '    try {\r\n'
                '      final r = await dio.get<Map<String, dynamic>>(\r\n'
                '        \'/billetera/$cuentaId/saldo\',\r\n'
                '      );\r\n'
                '      return SaldoBilletera.fromJson(r.data!);\r\n'
                '    } catch (e) {\r\n'
                '      throw errorDeDominio(e);\r\n'
                '    }\r\n'
                '  },\r\n'
                ');\r\n'
                '\r\n'
                '/// Qué es «vacío» para el saldo: una cuenta en cero **no es un error**, es una cuenta nueva.\r\n'
                'bool saldoEnCero(SaldoBilletera s) =>\r\n'
                '    s.disponible.monto == \'0.00\' && s.retenido.monto == \'0.00\';\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\cu15_emitir_extracto.dart': 'import \'package:aportaya_cliente_nucleofinanciero/aportaya_cliente_nucleofinanciero.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../dominio/cliente.dart\';\r\n'
                '\r\n'
                '/// CU-15 · emitir extracto y certificado de saldo. Es una consulta (sin efecto): no\r\n'
                '/// lleva clave de idempotencia. `hashArchivo` es la evidencia de integridad del PDF\r\n'
                '/// que emite el servidor; la app no genera ningún archivo por su cuenta.\r\n'
                'class ParametrosExtracto {\r\n'
                '  const ParametrosExtracto({\r\n'
                '    required this.cuentaId,\r\n'
                '    required this.desde,\r\n'
                '    required this.hasta,\r\n'
                '  });\r\n'
                '  final String cuentaId;\r\n'
                '  final DateTime desde;\r\n'
                '  final DateTime hasta;\r\n'
                '}\r\n'
                '\r\n'
                'final extractoProvider = FutureProvider.autoDispose\r\n'
                '    .family<SalidaExtracto, ParametrosExtracto>((ref, p) async {\r\n'
                '      final dio = ref.watch(dioProvider);\r\n'
                '      try {\r\n'
                '        final r = await DefaultApi(\r\n'
                '          dio,\r\n'
                '        ).emitirExtracto(cuentaId: p.cuentaId, desde: p.desde, hasta: p.hasta);\r\n'
                '        return r.data!;\r\n'
                '      } catch (e) {\r\n'
                '        throw errorDeDominio(e);\r\n'
                '      }\r\n'
                '    });\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\errores.dart': '/// Traducción de `AP-CU<NN>-<nn>` a lenguaje humano. La app no muestra el mensaje\r\n'
                '/// del backend: el texto que lee una persona es decisión de producto.\r\n'
                'library;\r\n'
                '\r\n'
                'import \'package:aportaya_diseno/errores.dart\';\r\n'
                '\r\n'
                'const Map<String, String> _catalogo = {\r\n'
                '  \'AP-CU01-01\':\r\n'
                '      \'Todavía no podemos abrir tu cuenta: falta verificar tus datos.\',\r\n'
                '  \'AP-CU01-03\': \'Ese celular ya tiene una cuenta. Si es tuyo, iniciá sesión.\',\r\n'
                '  \'AP-CU04-01\': \'El código no coincide. Revisalo e intentá de nuevo.\',\r\n'
                '  \'AP-CU04-04\': \'Demasiados intentos. Esperá una hora y volvé a probar.\',\r\n'
                '  \'AP-CU04-06\': \'Tu segundo factor no está activado. Pedí ayuda a soporte.\',\r\n'
                '  \'AP-CU21-03\': \'No pudimos cobrar. Revisá tu saldo e intentá de nuevo.\',\r\n'
                '  \'AP-VAL-03\': \'Este vale ya se usó. Cada vale se canjea una sola vez.\',\r\n'
                '};\r\n'
                '\r\n'
                'String mensajeDe(String codigo, {int? estado}) {\r\n'
                '  final conocido = _catalogo[codigo];\r\n'
                '  if (conocido != null) return conocido;\r\n'
                '  return switch (estado) {\r\n'
                '    401 => \'Tu sesión venció. Volvé a ingresar.\',\r\n'
                '    403 => \'No tenés acceso a esto.\',\r\n'
                '    404 => \'No encontramos lo que buscabas.\',\r\n'
                '    409 =>\r\n'
                '      \'Esta operación ya se hizo o cambió mientras tanto. Revisá el estado antes de repetirla.\',\r\n'
                '    _ => \'Algo salió mal de nuestro lado. Probá de nuevo en un momento.\',\r\n'
                '  };\r\n'
                '}\r\n'
                '\r\n'
                'class ErrorDeApi implements Exception, ErrorPresentable {\r\n'
                '  ErrorDeApi({\r\n'
                '    required this.codigo,\r\n'
                '    required this.trazaId,\r\n'
                '    required this.estado,\r\n'
                '  }) : mensaje = mensajeDe(codigo, estado: estado);\r\n'
                '\r\n'
                '  final String codigo;\r\n'
                '  @override\r\n'
                '  final String trazaId;\r\n'
                '  final int estado;\r\n'
                '  @override\r\n'
                '  final String mensaje;\r\n'
                '\r\n'
                '  @override\r\n'
                '  bool get sinConexion => false;\r\n'
                '\r\n'
                '  @override\r\n'
                '  String toString() => \'ErrorDeApi($codigo, $estado, traza $trazaId)\';\r\n'
                '}\r\n'
                '\r\n'
                'class ErrorDeRed implements Exception, ErrorPresentable {\r\n'
                '  @override\r\n'
                '  String get mensaje =>\r\n'
                '      \'No hay conexión. Te mostramos lo último que vimos; para operar hace falta señal.\';\r\n'
                '\r\n'
                '  @override\r\n'
                '  String? get trazaId => null;\r\n'
                '\r\n'
                '  @override\r\n'
                '  bool get sinConexion => true;\r\n'
                '\r\n'
                '  @override\r\n'
                '  String toString() => \'ErrorDeRed\';\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\foto_del_puente.dart': 'import \'package:dio/dio.dart\';\r\n'
                '\r\n'
                '/// Pide una foto al puente de cámara de desarrollo (`scripts/camara_del_mac.py`).\r\n'
                '///\r\n'
                '/// Vive acá y no en el adaptador porque **toda la red del cliente sale de\r\n'
                '/// `dominio/`** — lo comprueba `scripts/verificar_frontend.py`. Que el destino sea un\r\n'
                '/// servicio local de desarrollo no lo hace menos red.\r\n'
                '///\r\n'
                '/// Devuelve `null` si el puente no está levantado o si macOS todavía no concedió el\r\n'
                '/// permiso de cámara: quien llama ya tiene su camino alternativo.\r\n'
                'Future<List<int>?> pedirFotoAlPuente(String url, {Dio? cliente}) async {\r\n'
                '  try {\r\n'
                '    final r = await (cliente ?? Dio()).get<List<int>>(\r\n'
                '      url,\r\n'
                '      options: Options(\r\n'
                '        responseType: ResponseType.bytes,\r\n'
                '        // La captura arranca la cámara y descarta los primeros cuadros: tarda.\r\n'
                '        receiveTimeout: const Duration(seconds: 25),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '    final bytes = r.data;\r\n'
                '    return (bytes == null || bytes.isEmpty) ? null : bytes;\r\n'
                '  } on Object {\r\n'
                '    return null;\r\n'
                '  }\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\proteccion_de_pantalla_dinero.dart': 'import \'package:flutter/widgets.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../proveedores/proteccion_pantalla.dart\';\r\n'
                '\r\n'
                '/// Gate propio del carril: `screen_protector` activo en las rutas con saldo. El\r\n'
                '/// puerto ya lo declaró el carril M1 (`dominio/puertos/proteccion_pantalla.dart`);\r\n'
                '/// como `navegacion/` (congelado) no activa nada por ruta todavía, cada pantalla de\r\n'
                '/// billetera que muestra saldo se protege a sí misma al entrar y se libera al salir,\r\n'
                '/// para no dejarla prendida para toda la app.\r\n'
                'mixin ConProteccionDePantalla<T extends ConsumerStatefulWidget>\r\n'
                '    on ConsumerState<T> {\r\n'
                '  late final _proteccion = ref.read(proteccionPantallaProvider);\r\n'
                '\r\n'
                '  @override\r\n'
                '  void initState() {\r\n'
                '    super.initState();\r\n'
                '    WidgetsBinding.instance.addPostFrameCallback((_) {\r\n'
                '      if (mounted) _proteccion.activar();\r\n'
                '    });\r\n'
                '  }\r\n'
                '\r\n'
                '  @override\r\n'
                '  void dispose() {\r\n'
                '    _proteccion.desactivar();\r\n'
                '    super.dispose();\r\n'
                '  }\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\almacen_de_progreso.dart': 'import \'../tutoriales/modelo.dart\';\r\n'
                '\r\n'
                '/// **El puerto del progreso de los tutoriales.**\r\n'
                '///\r\n'
                '/// El motor no sabe si esto termina en el almacén del teléfono o en una tabla del\r\n'
                '/// servidor: pide leer, guardar y reiniciar, y alguien cumple.\r\n'
                '///\r\n'
                '/// Hoy el adaptador es local (`infraestructura/almacen_de_progreso_seguro.dart`)\r\n'
                '/// porque el backend todavía no tiene dónde guardar esto. El contrato remoto está\r\n'
                '/// documentado en `docs/Frontend/Motor de tutoriales.md` §11; el día que exista, se\r\n'
                '/// escribe otro adaptador y se cambia un `override`. Ninguna pantalla se entera.\r\n'
                'abstract interface class AlmacenDeProgreso {\r\n'
                '  /// Todo el progreso de quien tiene la sesión abierta en este teléfono.\r\n'
                '  Future<List<ProgresoDeTutorial>> leer();\r\n'
                '\r\n'
                '  /// Guarda una fila entera. Idempotente: la misma fila dos veces deja lo mismo.\r\n'
                '  Future<void> guardar(ProgresoDeTutorial progreso);\r\n'
                '\r\n'
                '  /// Borra el progreso de UN tutorial: reiniciar es empezar de cero.\r\n'
                '  Future<void> reiniciar(String tutorialId);\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\almacen_seguro.dart': '/// Lo que el dominio necesita del almacén seguro del dispositivo. La\r\n'
                '/// implementación (Keystore en Android, Keychain en iOS) vive en `infraestructura/`.\r\n'
                'abstract interface class AlmacenSeguro {\r\n'
                '  Future<String?> leer(String clave);\r\n'
                '  Future<void> guardar(String clave, String valor);\r\n'
                '  Future<void> borrar(String clave);\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\avisos_push.dart': '/// Registrar el dispositivo para avisos push y enterarse de qué pantalla abrir al\r\n'
                '/// tocar uno. **La bandeja de `/notificaciones/bandeja` es la fuente de verdad**\r\n'
                '/// (ADR-035): el push es un aviso de que hay algo nuevo, nunca el único lugar donde\r\n'
                '/// vive. La implementación (FCM en Android, APNs en iOS) vive en `infraestructura/`.\r\n'
                'abstract interface class AvisosPush {\r\n'
                '  /// El identificador que el backend usa para mandar avisos a este dispositivo.\r\n'
                '  /// `null` si el dispositivo no pudo registrarse (sin Google Play Services, por\r\n'
                '  /// ejemplo): la app sigue funcionando, solo que sin push — la bandeja alcanza.\r\n'
                '  Future<String?> token();\r\n'
                '\r\n'
                '  /// Avisos entrantes con la app abierta, con la ruta a la que hay que navegar.\r\n'
                '  Stream<String> get toques;\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\biometria.dart': '/// La biometría **confirma, no autentica** (regla 2 del shell, `planes/12`): quien\r\n'
                '/// autentica es el servidor con el token; la huella o el rostro solo desbloquean el\r\n'
                '/// envío de una operación ya armada. La implementación (BiometricPrompt en Android,\r\n'
                '/// LocalAuthentication en iOS) vive en `infraestructura/`.\r\n'
                'abstract interface class Biometria {\r\n'
                '  /// Si el dispositivo tiene sensor y ya hay biometría enrolada.\r\n'
                '  Future<bool> disponible();\r\n'
                '\r\n'
                '  /// Pide confirmación con [motivo] visible en el diálogo nativo (por ejemplo,\r\n'
                '  /// «Confirmá el envío de Bs 150»). `true` si la persona confirmó.\r\n'
                '  Future<bool> confirmar(String motivo);\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\camara.dart': '/// Capturar una imagen para el cotejo documental (CU-01) o el QR de recarga.\r\n'
                '/// **F2 la declara sin usuario todavía** — el primero es F3 — porque los siete\r\n'
                '/// puertos se fijan antes de la Ola F2 y un puerto nuevo en T5 sería un micro-PR al\r\n'
                '/// shell que los tres carriles de pantallas tendrían que rebasar (ficha `F2`).\r\n'
                'abstract interface class Camara {\r\n'
                '  /// La ruta del archivo capturado, o `null` si la persona canceló.\r\n'
                '  Future<String?> capturar({required bool esDocumento});\r\n'
                '\r\n'
                '  /// Una foto que **ya existe** en el teléfono, en vez de sacar una nueva.\r\n'
                '  ///\r\n'
                '  /// No es solo una comodidad: mucha gente ya tiene la foto de su carnet, y hay\r\n'
                '  /// teléfonos con la cámara rota. Y es la única vía en el simulador de iOS, que no\r\n'
                '  /// tiene cámara ni puede usar la del Mac —limitación de Apple, no del código—,\r\n'
                '  /// así que sin esto el flujo entero no se puede probar sin un teléfono real.\r\n'
                '  Future<String?> elegirDeLasFotos();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\conectividad.dart': '/// Estar «conectado a wifi» no es tener internet: el adaptador sondea el gateway.\r\n'
                'abstract interface class Conectividad {\r\n'
                '  Stream<bool> get cambios;\r\n'
                '  Future<bool> hayConexion();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\haptica.dart': '/// Retroalimentación táctil corta, para confirmar un toque de dinero o marcar un\r\n'
                '/// error de validación sin depender solo de la vista (accesibilidad).\r\n'
                'abstract interface class Haptica {\r\n'
                '  Future<void> exito();\r\n'
                '  Future<void> error();\r\n'
                '  Future<void> toqueLigero();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\puertos\\proteccion_pantalla.dart': '/// Bloquear captura y grabación de pantalla en las vistas con saldo o datos\r\n'
                '/// personales (regla 3 del shell). `go_router` lo activa y desactiva por ruta al\r\n'
                '/// entrar y salir — nunca queda prendido para toda la app.\r\n'
                'abstract interface class ProteccionPantalla {\r\n'
                '  Future<void> activar();\r\n'
                '  Future<void> desactivar();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\avance.dart': '/// **Las cuentas del avance, sin estado y sin IO.** Todo lo que el centro de ayuda\r\n'
                '/// muestra sale de acá, así se prueba con listas literales y sin levantar la app.\r\n'
                'library;\r\n'
                '\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// El progreso recién nacido de un tutorial que alguien acaba de abrir.\r\n'
                'ProgresoDeTutorial progresoInicial(\r\n'
                '  TutorialDefinicion t,\r\n'
                '  DateTime ahora, {\r\n'
                '  int repeticiones = 0,\r\n'
                '}) => ProgresoDeTutorial(\r\n'
                '  tutorialId: t.id,\r\n'
                '  version: t.version,\r\n'
                '  estado: EstadoDeProgreso.enProgreso,\r\n'
                '  pasoId: t.pasos.isEmpty ? null : t.pasos.first.id,\r\n'
                '  indice: 0,\r\n'
                '  iniciadoEn: ahora,\r\n'
                '  ultimaInteraccion: ahora,\r\n'
                '  repeticiones: repeticiones,\r\n'
                ');\r\n'
                '\r\n'
                '/// Mueve el progreso a un paso. No decide nada: eso es del motor.\r\n'
                'ProgresoDeTutorial enPaso(\r\n'
                '  ProgresoDeTutorial previo,\r\n'
                '  int indice,\r\n'
                '  String? pasoId,\r\n'
                '  DateTime ahora,\r\n'
                ') => previo.copiaCon(\r\n'
                '  estado: EstadoDeProgreso.enProgreso,\r\n'
                '  indice: indice,\r\n'
                '  pasoId: pasoId,\r\n'
                '  ultimaInteraccion: ahora,\r\n'
                ');\r\n'
                '\r\n'
                '/// Cierra el progreso con un desenlace. Completar suma una repetición; omitir no.\r\n'
                'ProgresoDeTutorial cerrado(\r\n'
                '  ProgresoDeTutorial previo,\r\n'
                '  EstadoDeProgreso estado,\r\n'
                '  DateTime ahora,\r\n'
                ') => previo.copiaCon(\r\n'
                '  estado: estado,\r\n'
                '  terminadoEn: ahora,\r\n'
                '  ultimaInteraccion: ahora,\r\n'
                '  repeticiones: estado == EstadoDeProgreso.completado\r\n'
                '      ? previo.repeticiones + 1\r\n'
                '      : previo.repeticiones,\r\n'
                ');\r\n'
                '\r\n'
                '/// **Cambió la versión del tutorial: lo que había ya no vale.**\r\n'
                '///\r\n'
                '/// El progreso viejo no se borra —se sigue sabiendo que esa persona lo hizo, y las\r\n'
                '/// repeticiones viajan— pero deja de contar como completado y el tutorial vuelve a\r\n'
                '/// ofrecerse. Es la estrategia conservadora a propósito: mostrar de más molesta;\r\n'
                '/// esconder un cambio que importa deja a alguien usando instrucciones viejas.\r\n'
                'bool vigente(ProgresoDeTutorial? p, TutorialDefinicion t) =>\r\n'
                '    p != null && p.version == t.version;\r\n'
                '\r\n'
                '/// El estado con el que se pinta la tarjeta, ya con la versión tenida en cuenta.\r\n'
                'EstadoDeProgreso estadoDe(ProgresoDeTutorial? p, TutorialDefinicion t) =>\r\n'
                '    p == null || !vigente(p, t) ? EstadoDeProgreso.pendiente : p.estado;\r\n'
                '\r\n'
                '/// Se continúa lo que quedó a medias en la versión que está corriendo, **lo haya\r\n'
                '/// dejado a medias o lo haya abandonado a propósito**.\r\n'
                '///\r\n'
                '/// Omitido también cuenta: al salir se promete «guardamos por qué paso ibas». Si\r\n'
                '/// después la tarjeta solo ofreciera empezar de cero, esa promesa sería mentira.\r\n'
                'bool sePuedeContinuar(ProgresoDeTutorial? p, TutorialDefinicion t) {\r\n'
                '  final estado = estadoDe(p, t);\r\n'
                '  return (estado == EstadoDeProgreso.enProgreso ||\r\n'
                '          estado == EstadoDeProgreso.omitido) &&\r\n'
                '      (p?.indice ?? 0) > 0;\r\n'
                '}\r\n'
                '\r\n'
                '/// Cuánto recorrió esta persona, de 0 a 1.\r\n'
                'double fraccionDe(ProgresoDeTutorial? p, TutorialDefinicion t) {\r\n'
                '  final estado = estadoDe(p, t);\r\n'
                '  if (estado == EstadoDeProgreso.completado) return 1;\r\n'
                '  if (t.pasos.isEmpty || p == null || estado == EstadoDeProgreso.pendiente) {\r\n'
                '    return 0;\r\n'
                '  }\r\n'
                '  return (p.indice / t.pasos.length).clamp(0, 1).toDouble();\r\n'
                '}\r\n'
                '\r\n'
                '/// Tutoriales completados sobre tutoriales disponibles. Lo omitido NO cuenta como\r\n'
                '/// hecho —omitir es «esto no lo necesito», no «ya lo sé»— pero tampoco se esconde.\r\n'
                'double avanceGeneral(\r\n'
                '  List<TutorialDefinicion> tutoriales,\r\n'
                '  Map<String, ProgresoDeTutorial> progresos,\r\n'
                ') {\r\n'
                '  if (tutoriales.isEmpty) return 0;\r\n'
                '  final hechos = tutoriales\r\n'
                '      .where((t) => estadoDe(progresos[t.id], t) == EstadoDeProgreso.completado)\r\n'
                '      .length;\r\n'
                '  return hechos / tutoriales.length;\r\n'
                '}\r\n'
                '\r\n'
                '/// Los requisitos que todavía le faltan a quien mira esta tarjeta.\r\n'
                'List<TutorialDefinicion> requisitosPendientes(\r\n'
                '  TutorialDefinicion t,\r\n'
                '  Map<String, TutorialDefinicion> porId,\r\n'
                '  Map<String, ProgresoDeTutorial> progresos,\r\n'
                ') => t.requisitos\r\n'
                '    .map((id) => porId[id])\r\n'
                '    .whereType<TutorialDefinicion>()\r\n'
                '    .where((r) => estadoDe(progresos[r.id], r) != EstadoDeProgreso.completado)\r\n'
                '    .toList();\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\escribano.dart': 'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../../proveedores/tutoriales.dart\';\r\n'
                'import \'avance.dart\';\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// **Quien escribe el avance, para que el motor no se ocupe de eso.**\r\n'
                '///\r\n'
                '/// Guardar no puede demorar el paso siguiente: si el almacén falla, el tutorial sigue y\r\n'
                '/// lo único que se pierde es poder retomarlo después. Por eso hay dos formas de guardar\r\n'
                '/// —esperando y sin esperar— y ninguna lanza hacia el recorrido.\r\n'
                'class EscribanoDeProgreso {\r\n'
                '  const EscribanoDeProgreso(this._ref);\r\n'
                '\r\n'
                '  final Ref _ref;\r\n'
                '\r\n'
                '  /// La fila de este tutorial, o una recién nacida si todavía no hay ninguna. Un\r\n'
                '  /// guardado anterior que falló no puede hacer que terminar un tutorial no cuente.\r\n'
                '  ProgresoDeTutorial de(TutorialDefinicion tutorial, DateTime ahora) =>\r\n'
                '      _ref.read(progresoDeTutorialesProvider)[tutorial.id] ??\r\n'
                '      progresoInicial(tutorial, ahora);\r\n'
                '\r\n'
                '  Future<void> guardar(ProgresoDeTutorial progreso) =>\r\n'
                '      _ref.read(progresoDeTutorialesProvider.notifier).guardar(progreso);\r\n'
                '\r\n'
                '  void sinEsperar(ProgresoDeTutorial progreso) => guardar(progreso).ignore();\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\estado_del_motor.dart': 'import \'dart:ui\' show Rect;\r\n'
                '\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// Lo que se ve de un recorrido en curso. Es inmutable: el motor publica estados\r\n'
                '/// nuevos, nadie muta el de adentro.\r\n'
                'class EstadoDelMotor {\r\n'
                '  const EstadoDelMotor({\r\n'
                '    this.tutorial,\r\n'
                '    this.indice = 0,\r\n'
                '    this.recuadro,\r\n'
                '    this.problema,\r\n'
                '    this.accionCumplida = false,\r\n'
                '    this.confirmandoSalida = false,\r\n'
                '  });\r\n'
                '\r\n'
                '  /// `null` es «no hay ningún tutorial corriendo», que es el estado normal.\r\n'
                '  final TutorialDefinicion? tutorial;\r\n'
                '  final int indice;\r\n'
                '\r\n'
                '  /// Dónde está el elemento resaltado. `null` es «todavía no» o «no está».\r\n'
                '  final Rect? recuadro;\r\n'
                '\r\n'
                '  /// Lo que impide seguir, en palabras. Se muestra en el globo, con reintento.\r\n'
                '  final String? problema;\r\n'
                '  final bool accionCumplida;\r\n'
                '  final bool confirmandoSalida;\r\n'
                '\r\n'
                '  bool get activo => tutorial != null;\r\n'
                '  PasoDeTutorial? get paso =>\r\n'
                '      tutorial == null || indice >= tutorial!.pasos.length\r\n'
                '      ? null\r\n'
                '      : tutorial!.pasos[indice];\r\n'
                '  int get total => tutorial?.pasos.length ?? 0;\r\n'
                '  bool get esUltimo => total > 0 && indice >= total - 1;\r\n'
                '\r\n'
                '  /// Se puede avanzar si el paso no pedía nada o si ya se hizo lo que pedía.\r\n'
                '  bool get puedeAvanzar =>\r\n'
                '      paso == null ||\r\n'
                '      paso!.accion.tipo == TipoDeAccion.ninguna ||\r\n'
                '      accionCumplida;\r\n'
                '\r\n'
                '  static const apagado = EstadoDelMotor();\r\n'
                '\r\n'
                '  EstadoDelMotor copiaCon({\r\n'
                '    TutorialDefinicion? tutorial,\r\n'
                '    int? indice,\r\n'
                '    Rect? recuadro,\r\n'
                '    String? problema,\r\n'
                '    bool? accionCumplida,\r\n'
                '    bool? confirmandoSalida,\r\n'
                '    bool limpiarRecuadro = false,\r\n'
                '    bool limpiarProblema = false,\r\n'
                '  }) => EstadoDelMotor(\r\n'
                '    tutorial: tutorial ?? this.tutorial,\r\n'
                '    indice: indice ?? this.indice,\r\n'
                '    recuadro: limpiarRecuadro ? null : (recuadro ?? this.recuadro),\r\n'
                '    problema: limpiarProblema ? null : (problema ?? this.problema),\r\n'
                '    accionCumplida: accionCumplida ?? this.accionCumplida,\r\n'
                '    confirmandoSalida: confirmandoSalida ?? this.confirmandoSalida,\r\n'
                '  );\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\modelo.dart': '/// **El contrato del motor de tutoriales de la app.** Solo datos: ni un `import` de\r\n'
                '/// Flutter, ni una línea de red. Un tutorial se declara en\r\n'
                '/// `pantallas/soporte/catalogo/` y el motor no conoce a ninguno en particular.\r\n'
                '///\r\n'
                '/// Es el mismo modelo que el del backoffice (`apps/backoffice/src/app/nucleo/\r\n'
                '/// tutoriales/tipos.ts`), en Dart: dos productos, un solo concepto de «tutorial».\r\n'
                'library;\r\n'
                '\r\n'
                '/// Lo que alguien puede hacer en la app, y que decide qué tutoriales le sirven.\r\n'
                '///\r\n'
                '/// **No es un permiso**: los permisos los aplica el servidor en cada llamada. Esto solo\r\n'
                '/// decide qué se OFRECE. Un tutorial jamás abre nada que la app no abriera igual.\r\n'
                'enum Capacidad {\r\n'
                '  /// Hay sesión abierta en este teléfono.\r\n'
                '  sesion,\r\n'
                '\r\n'
                '  /// Esta persona está habilitada para organizar un grupo (CU-90).\r\n'
                '  organizador,\r\n'
                '}\r\n'
                '\r\n'
                '/// Dónde se planta el globo respecto del elemento resaltado.\r\n'
                'enum PosicionDeGlobo { arriba, abajo, centro }\r\n'
                '\r\n'
                '/// Cuánto pide el tutorial de quien lo hace. Ordena el catálogo, no bloquea nada.\r\n'
                'enum Dificultad { inicial, intermedio, avanzado }\r\n'
                '\r\n'
                '/// Qué se espera que haga la persona antes de poder seguir.\r\n'
                '///\r\n'
                '/// **Ninguna acción escribe en el servidor.** El motor mira lo que ya pasa en la\r\n'
                '/// pantalla; nunca toca por su cuenta. Un tutorial no aporta, no paga y no retira.\r\n'
                'enum TipoDeAccion {\r\n'
                '  /// Se lee y se sigue con «Siguiente». Es lo normal.\r\n'
                '  ninguna,\r\n'
                '\r\n'
                '  /// Hay que tocar el elemento resaltado.\r\n'
                '  toque,\r\n'
                '\r\n'
                '  /// Hay que llegar a una ruta.\r\n'
                '  navegar,\r\n'
                '\r\n'
                '  /// Hay que hacer que algo aparezca (una hoja, un panel, una fila).\r\n'
                '  aparezca,\r\n'
                '}\r\n'
                '\r\n'
                '/// La acción esperada, con el dato que necesite según su tipo.\r\n'
                'class AccionEsperada {\r\n'
                '  const AccionEsperada.ninguna()\r\n'
                '    : tipo = TipoDeAccion.ninguna,\r\n'
                '      ruta = null,\r\n'
                '      ancla = null;\r\n'
                '  const AccionEsperada.toque({this.ancla})\r\n'
                '    : tipo = TipoDeAccion.toque,\r\n'
                '      ruta = null;\r\n'
                '  const AccionEsperada.navegar(String this.ruta)\r\n'
                '    : tipo = TipoDeAccion.navegar,\r\n'
                '      ancla = null;\r\n'
                '  const AccionEsperada.aparezca(String this.ancla)\r\n'
                '    : tipo = TipoDeAccion.aparezca,\r\n'
                '      ruta = null;\r\n'
                '\r\n'
                '  final TipoDeAccion tipo;\r\n'
                '  final String? ruta;\r\n'
                '  final String? ancla;\r\n'
                '}\r\n'
                '\r\n'
                '/// Un paso: una sola idea, un solo elemento, una sola acción.\r\n'
                'class PasoDeTutorial {\r\n'
                '  const PasoDeTutorial({\r\n'
                '    required this.id,\r\n'
                '    required this.titulo,\r\n'
                '    required this.descripcion,\r\n'
                '    this.ancla,\r\n'
                '    this.posicion = PosicionDeGlobo.abajo,\r\n'
                '    this.ruta,\r\n'
                '    this.orden,\r\n'
                '    this.accion = const AccionEsperada.ninguna(),\r\n'
                '    this.ayudaSiFalla,\r\n'
                '    this.espera = const Duration(seconds: 4),\r\n'
                '    this.bloquea = false,\r\n'
                '  });\r\n'
                '\r\n'
                '  final String id;\r\n'
                '  final String titulo;\r\n'
                '  final String descripcion;\r\n'
                '\r\n'
                '  /// El id de la `MarcaDeTutorial` que envuelve al elemento. Sin ancla, el globo va\r\n'
                '  /// centrado y habla de la pantalla entera.\r\n'
                '  final String? ancla;\r\n'
                '  final PosicionDeGlobo posicion;\r\n'
                '\r\n'
                '  /// La ruta donde este paso tiene sentido. El motor navega si hace falta.\r\n'
                '  final String? ruta;\r\n'
                '\r\n'
                '  /// Solo para validar el orden declarado; el orden real es el de la lista.\r\n'
                '  final int? orden;\r\n'
                '  final AccionEsperada accion;\r\n'
                '\r\n'
                '  /// Qué decirle a quien se quedó trabado.\r\n'
                '  final String? ayudaSiFalla;\r\n'
                '\r\n'
                '  /// Cuánto esperar a un elemento que llega después de una petición.\r\n'
                '  final Duration espera;\r\n'
                '\r\n'
                '  /// Con `true` no se puede tocar la app detrás del velo. Por omisión, sí se puede:\r\n'
                '  /// un paso que dice «tocá este botón» tiene que dejar tocarlo.\r\n'
                '  final bool bloquea;\r\n'
                '}\r\n'
                '\r\n'
                '/// Un tutorial completo. Es una constante, no una clase con lógica.\r\n'
                'class TutorialDefinicion {\r\n'
                '  const TutorialDefinicion({\r\n'
                '    required this.id,\r\n'
                '    required this.version,\r\n'
                '    required this.titulo,\r\n'
                '    required this.descripcion,\r\n'
                '    required this.categoria,\r\n'
                '    required this.dificultad,\r\n'
                '    required this.pasos,\r\n'
                '    this.ruta,\r\n'
                '    this.minutos,\r\n'
                '    this.obligatorio = false,\r\n'
                '    this.requisitos = const [],\r\n'
                '    this.siguiente,\r\n'
                '    this.requiere = const {},\r\n'
                '  });\r\n'
                '\r\n'
                '  final String id;\r\n'
                '\r\n'
                '  /// Cambiarla es decir «esto ya no es el mismo tutorial» (ver `avance.dart`).\r\n'
                '  final String version;\r\n'
                '  final String titulo;\r\n'
                '  final String descripcion;\r\n'
                '  final String categoria;\r\n'
                '  final Dificultad dificultad;\r\n'
                '  final List<PasoDeTutorial> pasos;\r\n'
                '\r\n'
                '  /// Dónde empieza; también enciende el acceso contextual de esa pantalla.\r\n'
                '  final String? ruta;\r\n'
                '  final int? minutos;\r\n'
                '  final bool obligatorio;\r\n'
                '\r\n'
                '  /// Ids de tutoriales que conviene haber hecho antes. Avisan, no bloquean.\r\n'
                '  final List<String> requisitos;\r\n'
                '\r\n'
                '  /// El que se ofrece al terminar este.\r\n'
                '  final String? siguiente;\r\n'
                '\r\n'
                '  /// Qué tiene que poder hacer alguien para que este tutorial le sirva. Un tutorial\r\n'
                '  /// que enseña a crear un grupo no se le ofrece a quien no puede organizar: sería\r\n'
                '  /// enseñarle a usar un botón que no va a ver.\r\n'
                '  final Set<Capacidad> requiere;\r\n'
                '}\r\n'
                '\r\n'
                '/// Qué le pasó a una persona con un tutorial.\r\n'
                'enum EstadoDeProgreso { pendiente, enProgreso, completado, omitido }\r\n'
                '\r\n'
                '/// La fila de progreso. Es lo que viaja al almacén, local hoy y remoto mañana.\r\n'
                'class ProgresoDeTutorial {\r\n'
                '  const ProgresoDeTutorial({\r\n'
                '    required this.tutorialId,\r\n'
                '    required this.version,\r\n'
                '    required this.estado,\r\n'
                '    required this.indice,\r\n'
                '    required this.iniciadoEn,\r\n'
                '    required this.ultimaInteraccion,\r\n'
                '    this.pasoId,\r\n'
                '    this.terminadoEn,\r\n'
                '    this.repeticiones = 0,\r\n'
                '  });\r\n'
                '\r\n'
                '  final String tutorialId;\r\n'
                '  final String version;\r\n'
                '  final EstadoDeProgreso estado;\r\n'
                '  final String? pasoId;\r\n'
                '  final int indice;\r\n'
                '  final DateTime iniciadoEn;\r\n'
                '  final DateTime? terminadoEn;\r\n'
                '  final DateTime ultimaInteraccion;\r\n'
                '  final int repeticiones;\r\n'
                '\r\n'
                '  ProgresoDeTutorial copiaCon({\r\n'
                '    EstadoDeProgreso? estado,\r\n'
                '    String? pasoId,\r\n'
                '    int? indice,\r\n'
                '    DateTime? terminadoEn,\r\n'
                '    DateTime? ultimaInteraccion,\r\n'
                '    int? repeticiones,\r\n'
                '  }) => ProgresoDeTutorial(\r\n'
                '    tutorialId: tutorialId,\r\n'
                '    version: version,\r\n'
                '    estado: estado ?? this.estado,\r\n'
                '    pasoId: pasoId ?? this.pasoId,\r\n'
                '    indice: indice ?? this.indice,\r\n'
                '    iniciadoEn: iniciadoEn,\r\n'
                '    terminadoEn: terminadoEn ?? this.terminadoEn,\r\n'
                '    ultimaInteraccion: ultimaInteraccion ?? this.ultimaInteraccion,\r\n'
                '    repeticiones: repeticiones ?? this.repeticiones,\r\n'
                '  );\r\n'
                '}\r\n'
                '',
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\dominio\\tutoriales\\motor.dart': 'import \'package:aportaya_diseno/moviles/anclas_de_tutorial.dart\';\r\n'
                'import \'package:flutter_riverpod/flutter_riverpod.dart\';\r\n'
                '\r\n'
                'import \'../../proveedores/tutoriales.dart\';\r\n'
                'import \'avance.dart\';\r\n'
                'import \'escribano.dart\';\r\n'
                'import \'estado_del_motor.dart\';\r\n'
                'import \'modelo.dart\';\r\n'
                '\r\n'
                '/// **El motor: dónde estoy, cómo sigo y qué hago si el paso no se puede dar.**\r\n'
                '///\r\n'
                '/// Es una máquina de estados; no dibuja nada. El velo, el globo y los gestos viven en\r\n'
                '/// `pantallas/soporte/capa_de_tutorial.dart`, y el catálogo en `pantallas/soporte/\r\n'
                '/// catalogo/`. Entre los tres no hay un solo `if` con el id de un tutorial concreto.\r\n'
                '///\r\n'
                '/// Tres reglas que no se negocian:\r\n'
                '/// 1. **No se traba.** Un objetivo que no aparece es un problema declarado con\r\n'
                '///    reintento y salida, nunca una pantalla congelada.\r\n'
                '/// 2. **No opera.** No toca botones, no envía formularios, no confirma nada.\r\n'
                '/// 3. **No inventa permisos.** Los tutoriales ya vienen filtrados por capacidad.\r\n'
                'class MotorDeTutoriales extends Notifier<EstadoDelMotor> {\r\n'
                '  late final _escribano = EscribanoDeProgreso(ref);\r\n'
                '  AnclasDeTutorial? _anclas;\r\n'
                '  void Function(String ruta)? _navegar;\r\n'
                '  String Function()? _ubicacion;\r\n'
                '\r\n'
                '  @override\r\n'
                '  EstadoDelMotor build() => EstadoDelMotor.apagado;\r\n'
                '\r\n'
                '  /// La capa visual le presta al motor lo que solo existe con un `BuildContext`: el\r\n'
                '  /// registro de anclas y la navegación. Se llama una vez, al montarse la capa.\r\n'
                '  void conectar({\r\n'
                '    required AnclasDeTutorial anclas,\r\n'
                '    required void Function(String ruta) navegar,\r\n'
                '    required String Function() ubicacion,\r\n'
                '  }) {\r\n'
                '    _anclas = anclas;\r\n'
                '    _navegar = navegar;\r\n'
                '    _ubicacion = ubicacion;\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Empieza de cero. Si ya se había hecho, las repeticiones viajan.\r\n'
                '  Future<void> iniciar(String tutorialId, {int repeticiones = 0}) async {\r\n'
                '    final tutorial = ref.read(registroDeTutorialesProvider).buscar(tutorialId);\r\n'
                '    if (tutorial == null || tutorial.pasos.isEmpty) return;\r\n'
                '    state = EstadoDelMotor(tutorial: tutorial);\r\n'
                '    await _escribano.guardar(\r\n'
                '      progresoInicial(tutorial, DateTime.now(), repeticiones: repeticiones),\r\n'
                '    );\r\n'
                '    await _ir(0);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Retoma donde quedó. Un índice fuera de rango vuelve al último paso posible.\r\n'
                '  Future<void> continuar(String tutorialId, ProgresoDeTutorial progreso) async {\r\n'
                '    final tutorial = ref.read(registroDeTutorialesProvider).buscar(tutorialId);\r\n'
                '    if (tutorial == null || tutorial.pasos.isEmpty) return;\r\n'
                '    state = EstadoDelMotor(tutorial: tutorial);\r\n'
                '    await _ir(progreso.indice.clamp(0, tutorial.pasos.length - 1));\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> avanzar() async {\r\n'
                '    if (!state.puedeAvanzar) {\r\n'
                '      state = state.copiaCon(\r\n'
                '        problema:\r\n'
                '            state.paso?.ayudaSiFalla ?? \'Hacé lo que pide el paso para seguir.\',\r\n'
                '      );\r\n'
                '      return;\r\n'
                '    }\r\n'
                '    if (state.esUltimo) return _terminar();\r\n'
                '    await _ir(state.indice + 1);\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> retroceder() async {\r\n'
                '    if (state.indice == 0) return;\r\n'
                '    await _ir(state.indice - 1);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Reintenta el paso actual: vuelve a buscar el objetivo desde cero.\r\n'
                '  Future<void> reintentar() => _ir(state.indice);\r\n'
                '\r\n'
                '  /// La acción que el paso pedía ya está hecha.\r\n'
                '  void marcarAccionCumplida() {\r\n'
                '    if (state.activo) state = state.copiaCon(accionCumplida: true);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Pide confirmación si va por la mitad; si recién empezó, cierra sin preguntar.\r\n'
                '  void pedirSalida() {\r\n'
                '    if (state.indice == 0 || state.esUltimo) {\r\n'
                '      omitir();\r\n'
                '      return;\r\n'
                '    }\r\n'
                '    state = state.copiaCon(confirmandoSalida: true);\r\n'
                '  }\r\n'
                '\r\n'
                '  void seguirEnElTutorial() {\r\n'
                '    state = state.copiaCon(confirmandoSalida: false);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Abandona: queda omitido, con el paso donde se fue. Se puede repetir después.\r\n'
                '  void omitir() {\r\n'
                '    final tutorial = state.tutorial;\r\n'
                '    if (tutorial != null) {\r\n'
                '      final ahora = DateTime.now();\r\n'
                '      _escribano.sinEsperar(\r\n'
                '        cerrado(\r\n'
                '          enPaso(\r\n'
                '            _escribano.de(tutorial, ahora),\r\n'
                '            state.indice,\r\n'
                '            state.paso?.id,\r\n'
                '            ahora,\r\n'
                '          ),\r\n'
                '          EstadoDeProgreso.omitido,\r\n'
                '          ahora,\r\n'
                '        ),\r\n'
                '      );\r\n'
                '    }\r\n'
                '    state = EstadoDelMotor.apagado;\r\n'
                '  }\r\n'
                '\r\n'
                '  Future<void> _terminar() async {\r\n'
                '    final tutorial = state.tutorial;\r\n'
                '    state = EstadoDelMotor.apagado;\r\n'
                '    if (tutorial == null) return;\r\n'
                '    final ahora = DateTime.now();\r\n'
                '    final hecho = _escribano\r\n'
                '        .de(tutorial, ahora)\r\n'
                '        .copiaCon(indice: tutorial.pasos.length);\r\n'
                '    await _escribano.guardar(\r\n'
                '      cerrado(hecho, EstadoDeProgreso.completado, ahora),\r\n'
                '    );\r\n'
                '  }\r\n'
                '\r\n'
                '  /// El corazón: navegar si el paso vive en otra pantalla, esperar al elemento y dejar\r\n'
                '  /// todo listo. Cada regreso temprano deja un problema declarado, nunca silencio.\r\n'
                '  Future<void> _ir(int indice) async {\r\n'
                '    final tutorial = state.tutorial;\r\n'
                '    if (tutorial == null || indice >= tutorial.pasos.length) return;\r\n'
                '    final paso = tutorial.pasos[indice];\r\n'
                '    state = state.copiaCon(\r\n'
                '      indice: indice,\r\n'
                '      accionCumplida: false,\r\n'
                '      limpiarProblema: true,\r\n'
                '      limpiarRecuadro: true,\r\n'
                '    );\r\n'
                '\r\n'
                '    _escribano.sinEsperar(\r\n'
                '      enPaso(\r\n'
                '        _escribano.de(tutorial, DateTime.now()),\r\n'
                '        indice,\r\n'
                '        paso.id,\r\n'
                '        DateTime.now(),\r\n'
                '      ),\r\n'
                '    );\r\n'
                '\r\n'
                '    // El primer paso hereda la ruta del tutorial: `ruta` de la definición es «dónde\r\n'
                '    // empieza». Sin esto, abrir desde Ayuda un tutorial de otra pantalla dejaba a la\r\n'
                '    // persona en Ayuda, con el primer ancla imposible de encontrar.\r\n'
                '    if (!_llegarA(paso.ruta ?? (indice == 0 ? tutorial.ruta : null))) return;\r\n'
                '    if (paso.ancla == null) return;\r\n'
                '    final recuadro = await _anclas?.esperar(paso.ancla!, paso.espera);\r\n'
                '    if (!state.activo || state.indice != indice) return;\r\n'
                '    if (recuadro == null) {\r\n'
                '      state = state.copiaCon(\r\n'
                '        problema: \'No encontramos «${paso.ancla}» en esta pantalla.\',\r\n'
                '      );\r\n'
                '      return;\r\n'
                '    }\r\n'
                '    state = state.copiaCon(recuadro: recuadro);\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Lleva a esa ruta si hace falta. `false` si no se pudo.\r\n'
                '  bool _llegarA(String? ruta) {\r\n'
                '    if (ruta == null) return true;\r\n'
                '    final actual = _ubicacion?.call() ?? \'\';\r\n'
                '    if (actual == ruta || actual.startsWith(\'$ruta/\')) return true;\r\n'
                '    if (_navegar == null) {\r\n'
                '      state = state.copiaCon(problema: \'No pudimos abrir «$ruta» desde acá.\');\r\n'
                '      return false;\r\n'
                '    }\r\n'
                '    _navegar!(ruta);\r\n'
                '    return true;\r\n'
                '  }\r\n'
                '\r\n'
                '  /// Vuelve a medir el elemento del paso actual: la pantalla se desplazó o cambió de\r\n'
                '  /// tamaño y el agujero tiene que seguirla.\r\n'
                '  void medirDeNuevo() {\r\n'
                '    final ancla = state.paso?.ancla;\r\n'
                '    if (!state.activo || ancla == null) return;\r\n'
                '    final recuadro = _anclas?.recuadroDe(ancla);\r\n'
                '    if (recuadro != null && recuadro != state.recuadro) {\r\n'
                '      state = state.copiaCon(recuadro: recuadro);\r\n'
                '    }\r\n'
                '  }\r\n'
                '}\r\n'
                '',
              ...
            }
     Which: has different length and has extra map key 'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\pantallas\\identidad\\pantalla_de_andamiaje.dart'
  
  package:matcher                                     expect
  package:flutter_test/src/widget_tester.dart 473:18  expect
  test\unidad\enchufe_de_rutas_test.dart 63:9         main.<fn>
  
00:02 +9 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/formatear_monto_test.dart: arma la cifra como la maqueta la muestra
00:02 +10 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/formatear_monto_test.dart: rechaza lo que no tiene la forma del contrato
00:02 +11 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/formatear_monto_test.dart: pasa los mismos vectores que el Monto de Angular, centavo por centavo
00:02 +12 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/gate_del_shell_test.dart: el paquete shared_preferences no se usa (ni se importa) en lib/
00:02 +13 -1: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/gate_del_shell_test.dart: Platform.is solo aparece dentro de infraestructura/
00:02 +13 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/gate_del_shell_test.dart: Platform.is solo aparece dentro de infraestructura/ [E]
  Expected: empty
    Actual: [
              'C:\\Users\\Usuario\\Downloads\\Entrypoint-GitHUb\\Pasanaku\\PasanakuBackend-richard-pr11/apps/movil/lib\\infraestructura\\plataforma.dart'
            ]
  Platform.is* solo en infraestructura/ (ADR-036): [C:\Users\Usuario\Downloads\Entrypoint-GitHUb\Pasanaku\PasanakuBackend-richard-pr11/apps/movil/lib\infraestructura\plataforma.dart]
  
  package:matcher                                     expect
  package:flutter_test/src/widget_tester.dart 473:18  expect
  test\unidad\gate_del_shell_test.dart 44:5           main.<fn>
  
00:02 +13 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/sesion_persiste_test.dart: la sesión sobrevive a cerrar y reabrir la app
00:02 +14 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/sesion_persiste_test.dart: cerrar sesión borra los dos tokens del almacén
00:03 +15 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: arranca en el primer paso, en progreso y sin terminar
00:03 +16 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: completar suma una repetición; omitir no
00:03 +17 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: la fracción sale del paso, y completado es siempre 1
00:03 +18 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: sin progreso, el tutorial está pendiente y en cero
00:03 +19 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: cambiar la versión invalida lo hecho: vuelve a ofrecerse
00:03 +20 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: lo abandonado a mitad de camino también se puede retomar
00:03 +21 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: se continúa solo lo que quedó a medias en la versión vigente
00:03 +22 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: el avance general no cuenta lo omitido como hecho
00:03 +23 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/avance_test.dart: los requisitos pendientes son los que todavía no se completaron
00:03 +24 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: el catálogo de la app no tiene ningún problema de configuración
00:03 +25 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: las rutas declaradas como conocidas son las que la app monta de verdad
00:03 +26 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: las rutas declaradas como conocidas son las que la app monta de verdad
00:03 +27 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/almacen_test.dart: guardar dos veces el mismo tutorial deja una sola fila, la última
00:03 +28 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: cada ancla de cada paso está marcada con MarcaDeTutorial en alguna pantalla
00:03 +29 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: cada ancla de cada paso está marcada con MarcaDeTutorial en alguna pantalla
00:03 +30 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: cada ancla de cada paso está marcada con MarcaDeTutorial en alguna pantalla
00:03 +31 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: cada ancla de cada paso está marcada con MarcaDeTutorial en alguna pantalla
00:03 +32 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: cada ancla de cada paso está marcada con MarcaDeTutorial en alguna pantalla
00:03 +33 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: hay al menos un tutorial importante y todos declaran cuánto duran
00:03 +34 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/catalogo_test.dart: ningún paso pide una acción que escriba en el servidor
00:03 +35 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: RegistroDeTutoriales sin capacidades solo se ve lo que no pide ninguna
00:03 +36 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: RegistroDeTutoriales con sesión aparece lo suyo, y lo de organizador sigue oculto
00:03 +37 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: RegistroDeTutoriales el tutorial de una pantalla es el de la ruta más específica
00:03 +38 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: RegistroDeTutoriales una pantalla sin tutorial no inventa uno
00:03 +39 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: RegistroDeTutoriales las categorías salen de lo disponible, no del catálogo entero
00:03 +40 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: componerLista ordena: importante, a medias, pendiente, omitido y al final lo hecho
00:03 +41 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: componerLista busca por título, descripción y categoría, sin importar mayúsculas
00:03 +42 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: componerLista el filtro de pendientes incluye los omitidos: dejarlo a medias no es hacerlo
00:03 +43 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: componerLista marca como continuable lo que quedó a mitad de camino
00:03 +44 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/registro_y_vista_test.dart: componerLista el recomendado es el primero sin requisitos pendientes; sin nada que hacer, no hay
00:03 +45 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: un catálogo sano no tiene nada que decir
00:03 +46 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta ids duplicados: uno pisa al otro
00:03 +47 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta un tutorial sin pasos
00:03 +48 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta un paso que no resalta nada ni navega
00:03 +49 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: un paso sin ancla pero que pide navegar es válido: el objetivo es la ruta
00:03 +50 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta una ruta que la app no sabe abrir, y acepta una subruta
00:03 +51 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta requisitos y siguientes que no existen
00:03 +52 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta una dependencia circular entre requisitos
00:03 +53 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: detecta pasos duplicados dentro de un tutorial
00:03 +54 -2: C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/tutoriales/validacion_test.dart: devuelve TODOS los problemas, no el primero
00:03 +55 -2: Some tests failed.

Failing tests:
  C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/enchufe_de_rutas_test.dart: agregar una pantalla vacía en un dominio no cambia ningún archivo fuera de él
  C:/Users/Usuario/Downloads/Entrypoint-GitHUb/Pasanaku/PasanakuBackend-richard-pr11/apps/movil/test/unidad/gate_del_shell_test.dart: Platform.is solo aparece dentro de infraestructura/
exit=1
