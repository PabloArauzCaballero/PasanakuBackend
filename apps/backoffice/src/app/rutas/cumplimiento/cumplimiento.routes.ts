import { Routes } from '@angular/router'

/** Las rutas del dominio cumplimiento. Vacías hasta que su carril las llene; el enchufe de
 * `app.routes.ts` ya las carga, así que llenarlas no toca el shell. */
export const rutasCumplimiento: Routes = []
