import 'package:go_router/go_router.dart';

/// Las rutas del dominio soporte. El reclamo (CU-52/53) vive en `pasanaku/` junto
/// al resto de la ficha F5 (`PantallaReclamoPendiente`, hueco de contrato
/// declarado); este directorio queda vacío hasta que aparezca una pantalla de
/// soporte que no sea un caso de uso de `pasanaku` (p. ej. centro de ayuda).
final List<RouteBase> rutasSoporte = [];
