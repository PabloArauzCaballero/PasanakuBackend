// Puente tipado sobre robots.mjs, que es lo que corre el script de build (Node, sin TS).
export { BUSCADORES_PERMITIDOS, ENTRENAMIENTO_BLOQUEADO, RUTAS_NO_INDEXABLES, robotsTxt } from './robots.mjs'
